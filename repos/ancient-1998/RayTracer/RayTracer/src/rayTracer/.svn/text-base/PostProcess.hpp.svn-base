#ifndef _rayTracer_PostProcess_hpp
#define _rayTracer_PostProcess_hpp

#include "RayTracerTypes.hpp"
#include "RenderTarget.hpp"
#include "Clock.hpp"

namespace rayTracer {

	class PostProcess {
	public:

		static void finalize(Clock& clock, const RenderParams& renderParams, RenderTarget& renderTarget, RenderTarget* pGlowBuffer);
	};
}

#endif