{"nodes":{"p1_1":{"t":"Why This Course Exists","ctx":"","body":"<p>This course exists because I cannot memorize rules and never could. If I understand the system that produces the rules, I remember it for years, because systems have structure and structure compresses.</p><p><a href=\"https://www.rust-lang.org/\">Rust</a> tutorials gave me rules such as &quot;one mutable reference or many immutable,&quot; &quot;moves invalidate the source,&quot; and &quot;lifetimes must not outlive their referent.&quot; Stack architecture, computability constraints, and Rice&#39;s theorem sit underneath these rules and explain every decision Rust&#39;s designers made. Dig into them and the rules stop requiring memorization because they become derivable. The <span class=\"ea\" data-t=\"p2_26\">borrow checker<span class=\"tt\"><span class=\"em-q\">How does the compiler enforce the constraint?</span></span></span> becomes a consequence of what memory is, what compilers can analyze, and what tradeoffs Rust chose.</p><p>I did not find that path in any existing course. <a href=\"https://doc.rust-lang.org/book/\">The Rust Book</a>, <a href=\"https://google.github.io/comprehensive-rust/\">Google's Comprehensive Rust</a>, <a href=\"https://github.com/nrc/r4cppp\">r4cppp</a>, <a href=\"https://www.codecademy.com/learn/rust-for-programmers\">Codecademy</a>, and <a href=\"https://www.udemy.com/courses/search/?src=ukw&q=rust\">Udemy</a> teach the rules well. This course derives them. SPACE × TIME × COORDINATES is a framework for memory bugs. Use-after-free, data races, and dangling references are failures in one of these three dimensions. The course builds the framework first, and the borrow checker falls out of it.</p><p><strong>Who this course is for</strong></p><p>This course is for experienced programmers, particularly those with a C++ background, who already have a mental model of systems programming. The course updates that model. The density is high and the chapters are short, because every sentence should add to what you already know.</p><p>If you are new to programming or new to systems languages, start with <a href=\"https://doc.rust-lang.org/book/\">The Rust Book</a>. It builds the mental model this course assumes you already have. Come back here when you want to understand the system underneath the rules.</p><p>We all want the same thing. Essence.</p><ul><li>&quot;This is exactly what I have been searching for!&quot;</li><li>&quot;Beautifully designed — understanding-first, ground-up approach&quot;</li><li>&quot;This is concise, accurate, and easy to use. I really like the explicit DAG of dependencies between concepts.&quot;</li><li>&quot;Physics creates distance. Distance forces copies. Copies require coherence. — What a quote! Very excited to go deeper.&quot;</li><li>&quot;The distilled essence of Rust&quot;</li></ul><p><strong>How to read this</strong></p><p>This is an interactive graph. Each section ends with an underlined <em>next.</em> that shows where you can go. Hover to see the options, then click to continue. Underlined words inside the text link to related topics. Hover for a preview, click to jump. You can also hover any section title to see its connections.</p><p>The sidebar on the left lists a guided &quot;Textbook&quot; walk that covers the full course in order. The sidebar tracks your progress and lets you jump to any section you&#39;ve visited. You can also explore freely. Every path through the graph is valid.</p>"},"p1_1b":{"t":"Familiar Syntax","ctx":"Most Rust syntax matches C++. Functions, structs, loops, and vectors work the same way.","body":"<div class=\"cb\"><code><span class=\"k\">fn</span> <span class=\"m\">main</span>() {\n    <span class=\"m\">println!</span>(<span class=\"s\">\"Hello, world!\"</span>);\n}</code></div><div class=\"cb\"><code><span class=\"k\">fn</span> <span class=\"m\">main</span>() {\n    <span class=\"k\">let</span> x = <span class=\"n\">5</span>;\n    <span class=\"k\">let</span> y = <span class=\"n\">10</span>;\n    <span class=\"m\">println!</span>(<span class=\"s\">\"{} + {} = {}\"</span>, x, y, x + y);\n}</code></div><div class=\"cb\"><code><span class=\"k\">fn</span> <span class=\"m\">add</span>(a: <span class=\"m\">i32</span>, b: <span class=\"m\">i32</span>) -&gt; <span class=\"m\">i32</span> {\n    a + b\n}\n\n<span class=\"k\">fn</span> <span class=\"m\">main</span>() {\n    <span class=\"k\">let</span> result = add(<span class=\"n\">3</span>, <span class=\"n\">4</span>);\n    <span class=\"m\">println!</span>(<span class=\"s\">\"Result: {}\"</span>, result);\n}</code></div><div class=\"cb\"><code><span class=\"k\">fn</span> <span class=\"m\">main</span>() {\n    <span class=\"k\">let</span> v = <span class=\"m\">vec!</span>[<span class=\"n\">1</span>, <span class=\"n\">2</span>, <span class=\"n\">3</span>, <span class=\"n\">4</span>, <span class=\"n\">5</span>];\n    <span class=\"m\">println!</span>(<span class=\"s\">\"Vector: {:?}\"</span>, v);\n    <span class=\"m\">println!</span>(<span class=\"s\">\"Length: {}\"</span>, v.len());\n}</code></div><div class=\"cb\"><code><span class=\"k\">struct</span> Point {\n    x: <span class=\"m\">i32</span>,\n    y: <span class=\"m\">i32</span>,\n}\n\n<span class=\"k\">fn</span> <span class=\"m\">main</span>() {\n    <span class=\"k\">let</span> p = Point { x: <span class=\"n\">10</span>, y: <span class=\"n\">20</span> };\n    <span class=\"m\">println!</span>(<span class=\"s\">\"Point at ({}, {})\"</span>, p.x, p.y);\n}</code></div><div class=\"cb\"><code><span class=\"k\">fn</span> <span class=\"m\">main</span>() {\n    <span class=\"k\">let</span> <span class=\"k\">mut</span> count = <span class=\"n\">0</span>;\n    <span class=\"k\">while</span> count &lt; <span class=\"n\">5</span> {\n        <span class=\"m\">println!</span>(<span class=\"s\">\"Count: {}\"</span>, count);\n        count += <span class=\"n\">1</span>;\n    }\n}</code></div><p>All of this compiles and runs. But some code that compiles in C++ <span class=\"ea\" data-t=\"p1_2\">does not compile in Rust<span class=\"tt\"><span class=\"em-q\">The compiler rejects code that looks right.</span></span></span>.</p>"},"p1_2":{"t":"Programs That Don't Compile","ctx":"Most Rust syntax matches C++. But some code that compiles in C++ does not compile in Rust.","body":"<p>These programs contain memory bugs that Rust rejects and C++ compiles.</p><div class=\"cb\"><code><span class=\"k\">let</span> v = <span class=\"m\">vec!</span>[<span class=\"n\">1</span>, <span class=\"n\">2</span>, <span class=\"n\">3</span>];\n<span class=\"k\">let</span> w = v;\n<span class=\"m\">println!</span>(<span class=\"s\">\"{:?}\"</span>, v);</code></div><div class=\"er\"><strong>error[E0382]:</strong> borrow of moved value: <code>v</code></div><p>The value moved to <code>w</code>, and <code>v</code> is gone. This is <span class=\"ea\" data-t=\"p2_14\">ownership<span class=\"tt\"><span class=\"em-q\">Why does assigning a value invalidate the original?</span></span></span> in action.</p><div class=\"cb\"><code><span class=\"k\">let</span> <span class=\"k\">mut</span> v = <span class=\"m\">vec!</span>[<span class=\"n\">1</span>, <span class=\"n\">2</span>, <span class=\"n\">3</span>];\n<span class=\"k\">let</span> r = &amp;v[<span class=\"n\">0</span>];\nv.push(<span class=\"n\">4</span>);\n<span class=\"m\">println!</span>(<span class=\"s\">\"{}\"</span>, r);</code></div><div class=\"er\"><strong>error[E0502]:</strong> cannot borrow <code>v</code> as mutable because it is also borrowed as immutable</div><p>A reference to <code>v[0]</code> exists while <code>push</code> tries to modify <code>v</code>. The <span class=\"ea\" data-t=\"p2_26\">borrowing rule<span class=\"tt\"><span class=\"em-q\">How does the compiler know this is wrong?</span></span></span> prevents this.</p><div class=\"cb\"><code><span class=\"k\">let</span> r;\n{\n    <span class=\"k\">let</span> x = <span class=\"n\">5</span>;\n    r = &amp;x;\n}\n<span class=\"m\">println!</span>(<span class=\"s\">\"{}\"</span>, r);</code></div><div class=\"er\"><strong>error[E0597]:</strong> <code>x</code> does not live long enough</div><p><code>x</code> is gone when the block ends. <code>r</code> would point to nothing. This is a <span class=\"ea\" data-t=\"p1_4\">dangling reference<span class=\"tt\"><span class=\"em-q\">C++ compiles it. Rust refuses.</span></span></span> caught at compile time.</p><div class=\"cb\"><code><span class=\"k\">let</span> <span class=\"k\">mut</span> s = <span class=\"m\">String</span>::from(<span class=\"s\">\"hello\"</span>);\n<span class=\"k\">let</span> r1 = &amp;<span class=\"k\">mut</span> s;\n<span class=\"k\">let</span> r2 = &amp;<span class=\"k\">mut</span> s;\n<span class=\"m\">println!</span>(<span class=\"s\">\"{}, {}\"</span>, r1, r2);</code></div><div class=\"er\"><strong>error[E0499]:</strong> cannot borrow <code>s</code> as mutable more than once at a time</div><p>Two mutable references to the same data. The compiler forbids this because two writers to the same memory is a <span class=\"ea\" data-t=\"p1_18\">data race<span class=\"tt\"><span class=\"em-q\">Two threads, one address, no synchronization.</span></span></span> waiting to happen.</p><div class=\"cb\"><code><span class=\"k\">fn</span> <span class=\"m\">get_string</span>() -&gt; &amp;<span class=\"m\">String</span> {\n    <span class=\"k\">let</span> s = <span class=\"m\">String</span>::from(<span class=\"s\">\"hello\"</span>);\n    &amp;s\n}</code></div><div class=\"er\"><strong>error[E0106]:</strong> missing lifetime specifier</div><p>The function tries to return a reference to a local variable. The local is destroyed when the function returns. The compiler demands a <span class=\"ea\" data-t=\"p2_16\">lifetime annotation<span class=\"tt\"><span class=\"em-q\">An explicit marker that tells the compiler how long a reference lives.</span></span></span> to verify the reference outlives its scope.</p>"},"p1_3":{"t":"Thread Safety Errors","ctx":"The compiler prevents data races too.","body":"<div class=\"cb\"><code><span class=\"k\">use</span> std::thread;\n<span class=\"k\">fn</span> <span class=\"m\">main</span>() {\n    <span class=\"k\">let</span> v = <span class=\"m\">vec!</span>[<span class=\"n\">1</span>, <span class=\"n\">2</span>, <span class=\"n\">3</span>];\n    thread::spawn(|| {\n        <span class=\"m\">println!</span>(<span class=\"s\">\"{:?}\"</span>, v);\n    });\n}</code></div><div class=\"er\"><strong>error[E0373]:</strong> closure may outlive the current function</div><p>The spawned thread might outlive <code>main</code>. The closure captures <code>v</code> by reference, but the reference could dangle when <code>main</code> returns. The fix is to <code>move</code> the value into the closure, transferring <span class=\"ea\" data-t=\"p2_14\">ownership<span class=\"tt\"><span class=\"em-q\">Where does the value go, and why can&#39;t the old scope see it?</span></span></span> to the thread.</p><div class=\"cb\"><code><span class=\"k\">use</span> std::rc::<span class=\"m\">Rc</span>;\n<span class=\"k\">use</span> std::thread;\n<span class=\"k\">fn</span> <span class=\"m\">main</span>() {\n    <span class=\"k\">let</span> rc = <span class=\"m\">Rc</span>::new(<span class=\"n\">5</span>);\n    thread::spawn(<span class=\"k\">move</span> || {\n        <span class=\"m\">println!</span>(<span class=\"s\">\"{}\"</span>, rc);\n    });\n}</code></div><div class=\"er\"><strong>error[E0277]:</strong> <code>Rc<i32></code> cannot be sent between threads safely</div><p><code>Rc</code> uses non-atomic reference counting. Sharing it across threads would corrupt the count. The type system encodes this. <code>Rc</code> does not implement <code>Send</code>. The compiler rejects the program at the type level, before any runtime check.</p><p>The same <span class=\"ea\" data-t=\"p2_26\">borrowing rule<span class=\"tt\"><span class=\"em-q\">How does one rule prevent both single-threaded and multi-threaded bugs?</span></span></span> that prevents single-threaded bugs also prevents <span class=\"ea\" data-t=\"p1_18\">data races<span class=\"tt\"><span class=\"em-q\">Two threads accessing the same memory, at least one writing.</span></span></span>.</p>"},"p1_4":{"t":"The Dangling Reference","ctx":"One rejected program examined in detail.","body":"<div class=\"cb\"><code><span class=\"k\">fn</span> <span class=\"m\">main</span>() {\n    <span class=\"k\">let</span> r;\n    {\n        <span class=\"k\">let</span> x = <span class=\"n\">5</span>;\n        r = &amp;x;\n    }\n    <span class=\"m\">println!</span>(<span class=\"s\">\"{}\"</span>, r);\n}</code></div><div class=\"er\"><strong>error[E0597]:</strong> <code>x</code> does not live long enough</div><p><code>r</code> refers to <code>x</code>, and the block ends. <code>x</code> is gone, and <code>r</code> points to nothing.</p><p>The C++ equivalent compiles and runs. It prints garbage, or crashes, or prints 5 by accident. The C++ standard leaves this behavior undefined.</p><div class=\"cb\"><code><span class=\"c\">// C++</span>\n<span class=\"k\">int</span> main() {\n    <span class=\"k\">int</span>* r;\n    {\n        <span class=\"k\">int</span> x = <span <span class=\"k\">class</span>=\"n\">5</span>;\n        r = &amp;x;\n    }\n    <span class=\"k\">printf</span>(<span class=\"s\">\"%d\\n\"</span>, *r);\n}</code></div><p>Rust rejects the program before it runs. The compiler tracks where every reference points and when the target is <span class=\"ea\" data-t=\"p2_2\">reclaimed<span class=\"tt\"><span class=\"em-q\">The space is returned to the stack. The bytes are still there, but the allocation has ended.</span></span></span>. A reference that outlives its target is a <span class=\"ea\" data-t=\"p1_16\">dangling pointer<span class=\"tt\"><span class=\"em-q\">One of the most common memory bugs in C and C++.</span></span></span>.</p>"},"ds_0":{"t":"Why Distributed Systems Theory","ctx":"Data must be distributed. Distributed data decoheres. Preventing decoherence is what distributed systems theory fundamentally solves.","body":"<p>Physics forces data to be distributed. Cores have caches because RAM is too far away. Threads have local copies because the bus is too slow. Servers have replicas because continents are too wide. Every layer of computing distributes data to survive distance.</p><p>Distributed data decoheres. A write to the source makes every copy stale. The copies do not know the source changed, and without a protocol the system silently diverges.</p><p>Preventing decoherence is the fundamental problem of distributed systems theory. The theory produced a constraint, many readers or one writer, and proved it optimal. The same constraint appears in hardware cache coherence, database locking, and Rust&#39;s borrow checker. Each layer enforces it differently, and none escape it.</p>"},"p1_12":{"t":"Distance Forces Copies","ctx":"Physics creates distance. Distance forces copies. Copies require coherence.","body":"<p>Physics creates distance. A CPU core and RAM are centimeters apart. A user and a server are continents apart. A thread and the heap are separated by a shared bus.</p><p>Distance makes access slow. Every layer of computing makes copies. Caches copy RAM. CDN edges copy origin servers. Database replicas copy the primary. References copy addresses.</p><p>Every copy is derived data. A representation computed from a source carries a sync obligation. A change to the source makes the copy stale. Keeping copies consistent with their source is a coherence problem. Every layer solves it with a different strategy.</p><table><thead><tr><th>Layer</th><th>Source</th><th>Copy</th><th>Sync Strategy</th></tr></thead><tbody><tr><td>CPU cache</td><td>RAM</td><td>L1/L2/L3 line</td><td>MESI protocol</td></tr><tr><td>Compiler</td><td>Memory</td><td>Register</td><td>Register allocation</td></tr><tr><td>Language</td><td>Original value</td><td>Reference/alias</td><td>Ownership, locks, GC</td></tr><tr><td>Thread</td><td>Shared heap</td><td>Thread-local</td><td>Mutex, channels, atomics</td></tr><tr><td>Process</td><td>Shared memory</td><td>Process-local</td><td>IPC, message passing</td></tr><tr><td>Database</td><td>Primary</td><td>Replica</td><td>Replication protocol</td></tr><tr><td>Network</td><td>Origin server</td><td>CDN edge</td><td>TTL, invalidation</td></tr></tbody></table><p>Each layer resolves staleness differently. The pattern is the same. Cache invalidation, stale replicas, dangling pointers, and torn register reads all follow from the same mechanism.</p><p>Physics creates distance. Distance forces copies. Copies require coherence.</p>"},"p1_13":{"t":"Three Primitives","ctx":"The coherence problem has three dimensions. Where copies live. When they change. How you reach them.","body":"<p>The coherence problem has three dimensions. Where copies live. When they change. How you reach them.</p><p><strong>SPACE.</strong> Where data lives. Local store, main store, stack, heap, register, cache line, disk, remote server. Memory hierarchy is a ladder of spaces with different latencies.</p><p><strong>TIME.</strong> When things happen. The write that makes copies stale is a time event. Scope entry to scope exit is a time interval. Compile-time vs runtime. Sequential vs parallel. A value&#39;s lifetime.</p><p><strong>COORDINATES.</strong> How we refer to data. Names at compile-time, addresses at runtime, references, pointers. Coordinates tell you where space is.</p><p>The rest of the course abbreviates these as <strong>S×T×C</strong>. Bugs are failures in one or more of the three dimensions. Fixes adjust one or more of the three.</p><p><strong>State</strong> is data that changes over TIME. Stateless computation has no coherence problem. Most coherence problems involve state.</p><p><strong>Derived data</strong> propagates coherence problems. A coordinate to a value is derived data. A cached copy is derived data. The source lives in one SPACE, the derived copy in another, and TIME determines when they fall out of sync.</p>"},"p1_5b":{"t":"Copies Go Stale","ctx":"Derived data carries a sync obligation. The holders do not know when they fall out of sync. This is the coherence problem.","body":"<p>Every copy is derived data, and derived data goes stale. The holders do not know.</p><p>A coordinate to a value is derived data. A cached copy is derived data. The source lives in one SPACE, the derived copy in another, and TIME determines when they fall out of sync.</p><p>This is the coherence problem. Any system with mutable shared data has it. Distributed systems theory studies it in its general form. The theory produced a solution and proved it optimal.</p>"},"p1_8":{"t":"Readers and Writers","ctx":"Many readers or one writer. The surprise is not the rule. The surprise is that nothing more permissive exists.","body":"<p>The rule, many readers or one writer, is obvious. Readers do not change state, so they cannot conflict with each other. Writers change state, so they conflict with everything. Courtois, Heymans, and Parnas formalized this in 1971. The result is unsurprising.</p><p>What is not obvious is that this is the ceiling. Fifty years of research attempted to find something more permissive, and none succeeded.</p><p>MVCC lets readers proceed during a write — but the reader sees a frozen snapshot, not live data. CRDTs permit concurrent writes that merge — but only for commutative operations like addition, not arbitrary mutation. Software transactional memory allows speculative concurrent writes — but aborts and retries when a conflict is detected. Every alternative either restricts what operations are allowed, gives readers old data instead of current data, or speculates and rolls back on failure. None break the rule, but they escape it by changing the operations, the data model, or the correctness guarantee.</p><p>Kung and Papadimitriou proved optimality formally in 1979. For arbitrary read/write operations, no scheduler can safely permit more concurrency than the readers-writers policy without semantic knowledge of what the operations do.</p><table><thead><tr><th>Year</th><th>Domain</th><th>Form</th></tr></thead><tbody><tr><td>1971</td><td>Concurrent programming</td><td>Readers-writers (Courtois, Heymans, Parnas)</td></tr><tr><td>1975</td><td>Databases</td><td>Shared/exclusive locking (Gray)</td></tr><tr><td>1979</td><td>Optimality proof</td><td>No better scheduler exists (Kung, Papadimitriou)</td></tr><tr><td>1984</td><td>Hardware</td><td>MESI cache coherence (Papamarcos, Patel)</td></tr><tr><td>~2012</td><td>Programming languages</td><td><code>&T</code> / <code>&mut T</code> (Rust)</td></tr></tbody></table>"},"p1_8b":{"t":"No Runtime","ctx":"Every alternative to the readers-writers rule requires a runtime. The compiler has none. That is why the borrow checker enforces this rule and no other.","body":"<p>Every alternative to the readers-writers rule requires a runtime.</p><p>MVCC creates snapshots. Someone must allocate, track, and garbage-collect old versions. CRDTs merge concurrent writes. Someone must detect concurrency and apply the merge function. STM speculates and retries on conflict. Someone must log operations, detect conflicts at commit time, and re-execute failed transactions.</p><p>Rust&#39;s design goal is to solve the coherence problem at compile time with zero runtime cost, no garbage collector, and no tracking overhead. The readers-writers rule is the only coherence policy a compiler can enforce by reading the program without executing it.</p><p>That is why the <span class=\"ea\" data-t=\"p2_26\">borrow checker<span class=\"tt\"><span class=\"em-q\">The compile-time rule that prevents these bugs.</span></span></span> enforces this rule and no other.</p>"},"p1_5":{"t":"Caches and Staleness","ctx":"The hardware enforces the same rule. Every core has its own cache. Writes invalidate every other copy.","body":"<p>The hardware enforces the rule, many readers or one writer, on every CPU.</p><p>Every core in a modern CPU has its own cache, a local copy of data from RAM. Multiple cores can cache the same address. Reads produce no conflict because every copy holds the same value. Writes invalidate every other copy. The moment one core writes, every other cached copy of that address is stale.</p><p>The hardware enforces coherence with a protocol. A write forces exclusive access. The protocol invalidates all other copies, and all other cores trying to access that address stall. The protocol serializes all access to that address until the write completes.</p><p>At any moment: <strong>many caches can hold a read-only copy, or one cache can hold a writable copy. Never both.</strong></p><div class=\"cb\"><code>Safe:                           Forbidden:\n\nCore 0   Core 1   Core 2       Core 0   Core 1\n┌─────┐  ┌─────┐  ┌─────┐     ┌─────┐  ┌─────┐\n│read │  │read │  │read │     │write│  │read │\n│copy │  │copy │  │copy │     │     │  │copy │\n└─────┘  └─────┘  └─────┘     └─────┘  └─────┘\n   ▲        ▲        ▲           ▲        ▲\n   └────────┼────────┘           │  stale! │\n         shared                  └────┬────┘\n                                   conflict</code></div><p>The protocol operates on 64-byte blocks called cache lines. The same state governs two unrelated variables in the same block. Two cores writing to different variables in the same block forces serialization, even though the variables are independent. The hardware calls this false sharing.</p>"},"p1_6":{"t":"What Happens When Two Cores Touch the Same Address","ctx":"Two cores, one address. What the hardware does to keep cached copies consistent.","body":"<p>Two cores share one address. Core 0 reads it, Core 1 reads it, and both hold a cached copy with the current value. No conflict exists.</p><p>Core 0 writes, and its copy now holds the new value. Core 1&#39;s copy still holds the old value, and Core 1 does not know anything changed.</p><p>The protocol must intervene. The protocol notifies every other core holding a copy and invalidates their copies. Only then does Core 0 proceed.</p><p>Core 1 must re-fetch the value to read afterward, either from Core 0&#39;s cache or from RAM, depending on whether Core 0 has flushed.</p><p>Core 1 acquires exclusive access to write. The protocol invalidates Core 0&#39;s copy. Now Core 1 is the sole holder. The write proceeds.</p><p>At any moment, the protocol enforces one rule: <strong>either many cores hold a read-only copy, or exactly one core holds a writable copy. Never both.</strong></p><p>Two readers are safe. One writer with no readers is safe. One writer plus any reader is forbidden. Two writers are forbidden.</p>"},"p1_9":{"t":"Deriving the States","ctx":"The constraint says: write permission and the reader set cannot coexist for the same entry.","body":"<p>The <span class=\"ea\" data-t=\"p1_5\">cache coherence protocol<span class=\"tt\"><span class=\"em-q\">Caches enforce the rule at runtime.</span></span></span> is a distributed systems protocol. Strip the hardware names. Cores are processes, caches are local stores, RAM is the main store, the coherence controller is a directory. The protocol enforces the readers-writers rule using token-based coordination (Suzuki-Kasami 1985, Raymond 1989).</p><p>The directory tracks two things per entry. A <strong>token</strong> grants write permission to at most one holder. A <strong>reader set</strong> lists processes with read-only copies. Token and reader set cannot coexist for the same entry.</p><p>The four legal states are the four allowed combinations of token and reader set:</p><table><thead><tr><th>Token?</th><th>Reader set?</th><th>What it means</th><th>Name</th></tr></thead><tbody><tr><td>Held, written</td><td>Empty</td><td>One copy. Sole authority. Main store is stale.</td><td>Modified</td></tr><tr><td>Held, not written</td><td>Empty</td><td>One copy. Can write silently — no readers to revoke.</td><td>Exclusive</td></tr><tr><td>Not held</td><td>Populated</td><td>Many copies. All match main store. Read-only.</td><td>Shared</td></tr><tr><td>Not held</td><td>Not in set</td><td>No copy. Must request on next access.</td><td>Invalid</td></tr></tbody></table><p>The initials spell MESI. Two consequences follow from these states.</p><p><strong>Why flush before sharing?</strong> Shared copies can be silently discarded. If the main store were still stale and all shared copies were silently discarded, the value would be lost forever. Flushing before sharing ensures the main store can always recover the value.</p><p><strong>Why can shared copies be discarded silently?</strong> All shared copies match the main store. The value can always be re-fetched. No message is needed. The directory&#39;s reader set can be imprecise. It may list processes that have already silently discarded their copies.</p>"},"p1_10":{"t":"The Protocol","ctx":"The whole protocol is: write permission and the reader set cannot coexist for the same entry.","body":"<p>The whole protocol is one invariant. Write permission and the reader set cannot coexist for the same entry. Every message exchange either avoids the conflict or resolves it by revoking one side.</p><p><strong>Read, process holds nothing.</strong> Request goes to the directory. If no one holds anything, the directory supplies the value from the main store and grants a read copy. If readers exist, the process joins the reader set. If another process holds the token, the directory asks the holder to flush, surrender the token, and join the reader set. The requester joins too. Both shared.</p><p><strong>Write, process holds nothing.</strong> The process needs the token. The directory revokes all readers with invalidation messages and waits for acknowledgments. Then it grants the token. The process can now write repeatedly with zero messages.</p><p><strong>Write, process holds a shared copy.</strong> The process needs the token. The directory revokes all other readers. Grants the token. No data transfer is needed because the process already has the value.</p><p><strong>Write, process already holds the token.</strong> No messages. Write directly. This can happen millions of times with zero protocol traffic.</p><p><strong>Another process reads an entry held as modified.</strong> The directory asks the holder to flush to main store, surrender the token, keep a read copy. The requester gets a read copy. Both shared.</p><p><strong>A shared copy is evicted.</strong> Silently discarded. No message. Safe because the main store is current.</p><p><strong>A modified entry is evicted.</strong> The main store is stale. Silent discard would lose the value. The process must flush and surrender the token. This is the only eviction that requires a message.</p><p>Every transition preserves the <span class=\"ea\" data-t=\"p1_8\">constraint<span class=\"tt\"><span class=\"em-q\">Many readers or one exclusive writer.</span></span></span>, and the protocol is complete.</p>"},"p1_10b":{"t":"Why the Protocol Looks Like This","ctx":"Four states, one rule. Each state records what you can do without asking anyone.","body":"<p>The protocol has four states. Each state records what this process can do right now without asking anyone.</p><p><strong>Sole writer.</strong> You have been writing, no one else has a copy. Keep writing. <strong>Sole reader.</strong> You are the only one with a copy but have not written yet. Your first write needs no permission. <strong>One of many readers.</strong> Others are reading too. You can read, but writing means they have to go first. <strong>Nothing.</strong> You have nothing and must request access.</p><p>The hardware calls these Modified, Exclusive, Shared, Invalid. The initials spell MESI. Four states derive from one rule, and the rest is bookkeeping.</p>"},"p1_21":{"t":"The Lineage","ctx":"The constraint is: many readers or one exclusive writer. Four decades, four domains, the same answer.","body":"<p>The rule is many readers or one writer.</p><p>Rust&#39;s <span class=\"ea\" data-t=\"p2_26\">borrowing rules<span class=\"tt\"><span class=\"em-q\">The constraint at compile time.</span></span></span> enforce it at compile time. <code>&T</code> is shared and read-only. <code>&mut T</code> is exclusive and writable. The compiler rejects programs that violate this.</p><p>The same rule, at every layer:</p><table><thead><tr><th>Layer</th><th>Year</th><th>Form</th></tr></thead><tbody><tr><td>Concurrent programming</td><td>1971</td><td>Readers/writers with semaphores (Courtois, Heymans, Parnas)</td></tr><tr><td>Databases</td><td>1975</td><td>Shared/exclusive locking (Gray, Eswaran et al.)</td></tr><tr><td>Optimality proof</td><td>1979</td><td>No better scheduler exists (Kung, Papadimitriou)</td></tr><tr><td>Hardware</td><td>1984</td><td>Cache coherence, MESI states (Papamarcos, Patel)</td></tr><tr><td>C++ memory model</td><td>2011</td><td>Happens-before, data race freedom</td></tr><tr><td>Rust</td><td>~2012</td><td>Borrowing rule, <code>&T</code> / <code>&mut T</code></td></tr></tbody></table><p>The MESI protocol enforces it at runtime, for every address, on every CPU. The borrow checker enforces it at compile time, for every reference, in every Rust program. The cache coherence controller is a runtime borrow checker. The borrow checker is a compile-time coherence protocol.</p><p>What does distributed systems theory have to do with it? Everything.</p>"},"p1_11":{"t":"Shared Memory Is Message Passing","ctx":"Writing to shared memory triggers a message-passing protocol underneath.","body":"<p>In the base protocol, all writes to the same entry are fully serialized. Only one process can hold the token at any moment. Every other process that wants to write must wait. Writes to different entries proceed in parallel because each entry has independent state, but writes to the same entry are strictly sequential.</p><p>The &quot;sharing&quot; in &quot;shared memory&quot; is reading only. Message passing between processes serializes writing.</p><p>Consider two ways to pass a value from thread A to thread B.</p><p>Go, using a channel:</p><div class=\"cb\"><code>ch &lt;- 42        // thread A sends\nval := &lt;-ch     // thread B receives</code></div><p>Java, using a shared variable:</p><div class=\"cb\"><code>x = 42;         // thread A writes\nval = x;        // thread B reads</code></div><p>The Go programmer sees message passing. The Java programmer sees shared memory.</p><p>Both produce the same protocol traffic underneath. The assignment <code>x = 42</code> triggers a request for write permission, invalidation of all readers, acknowledgments, and a grant. Multiple messages for one assignment statement. &quot;Shared memory&quot; is an API over a message-passing protocol.</p><p>Lauer and Needham proved in 1978 that shared-memory concurrency and message-passing concurrency are formally equivalent. Any program in one maps to a program in the other. The coherence protocol is that equivalence, running underneath every &quot;shared&quot; variable.</p>"},"p1_14":{"t":"Bugs as Interaction Failures","ctx":"Every memory bug maps to a specific interaction between primitives.","body":"<p>Interactions between the three primitives produce every category of memory bug.</p><p><strong>SPACE × TIME.</strong> When memory exists. Allocation, deallocation, lifetimes, scope.</p><p><strong>SPACE × COORDINATES.</strong> How many paths lead to memory. Aliasing, ownership, null.</p><p><strong>TIME × COORDINATES.</strong> When a reference is valid. Scope, shadowing, drop order.</p><table><thead><tr><th>Bug</th><th>Primitives</th><th>Breakdown</th></tr></thead><tbody><tr><td><span class=\"ea\" data-t=\"p1_15\">Use-after-free<span class=\"tt\"><span class=\"em-q\">A heap pointer followed after the heap is freed.</span></span></span></td><td>S×T×C</td><td>Use (COORD) after (TIME) free (SPACE)</td></tr><tr><td><span class=\"ea\" data-t=\"p1_16\">Dangling pointer<span class=\"tt\"><span class=\"em-q\">A reference that outlives the thing it points to.</span></span></span></td><td>T×C</td><td>Pointer (COORD) outlives target (TIME)</td></tr><tr><td><span class=\"ea\" data-t=\"p1_17\">Double free<span class=\"tt\"><span class=\"em-q\">The allocator&#39;s bookkeeping is corrupted.</span></span></span></td><td>S×T</td><td>Free (SPACE) twice (TIME)</td></tr><tr><td><span class=\"ea\" data-t=\"p1_18\">Data race<span class=\"tt\"><span class=\"em-q\">Two threads writing the same memory with no synchronization.</span></span></span></td><td>S×T×C</td><td>Data (SPACE) race (TIME) via aliases (COORD)</td></tr><tr><td>Buffer overflow</td><td>S×C</td><td>Coord exceeds bounds of SPACE</td></tr><tr><td>Uninitialized read</td><td>S×T×C</td><td>Read (COORD) before (TIME) init (SPACE)</td></tr><tr><td>Memory leak</td><td>S×T</td><td>Space outlives all coords (TIME)</td></tr></tbody></table>"},"p1_15":{"t":"Use-After-Free","ctx":"Use (COORDINATES) after (TIME) free (SPACE).","body":"<div class=\"cb\"><code>TIME  t0        t1              t2          t3\nEVENTS  create heap  p ← addr(heap)  free heap   &gt;at_addr(p)&lt;\n────────────────────────────────────────────────────────────────────────\nHEAP    ████████████████████████████████\nCOORD (p)            ████████████████░░░░░░░░░░░░░░░░▓▓▓▓░░░░░░░░\n                                                      &gt;BUG&lt;</code></div><p>The heap is freed at t2. The coordinate <code>p</code> still exists. At t3, the program follows <code>p</code> to memory that no longer belongs to it.</p><p>In C++, this compiles and runs. The behavior is undefined. In Rust, <span class=\"ea\" data-t=\"p2_14\">ownership<span class=\"tt\"><span class=\"em-q\">One owner. When the owner goes out of scope, the value is dropped.</span></span></span> prevents any coordinate from outliving the space it points to.</p>"},"p1_16":{"t":"Dangling Pointer","ctx":"Pointer (COORDINATES) dangles (TIME: target gone).","body":"<div class=\"cb\"><code>TIME  t0        t1              t2            t3\nEVENTS  create x  r ← addr(x)  scope ends    &gt;at_addr(r)&lt;\n────────────────────────────────────────────────────────────────────────\nSPACE (x)  ████████████████████████████████\nCOORD (r)            ████████████████░░░░░░░░░░░░░░░░▓▓▓▓░░░░░░░░\n                                                      &gt;BUG&lt;</code></div><p>The variable <code>x</code> lives on the stack. The scope ends at t2. The space is reclaimed. The coordinate <code>r</code> now points to reclaimed space.</p><p>This is the same bug from <span class=\"ea\" data-t=\"p1_4\">the first chapter<span class=\"tt\"><span class=\"em-q\">Rust rejects it at compile time. C++ prints garbage.</span></span></span>. The timeline makes the mechanism precise. TIME destroys the SPACE, but the COORDINATE survives.</p>"},"p1_17":{"t":"Double Free","ctx":"Free (SPACE) twice (TIME).","body":"<div class=\"cb\"><code>TIME  t0        t1              t2                t3\nEVENTS  create heap  p ← addr(heap)  free_at_addr(p)  &gt;free_at_addr(p)&lt;\n────────────────────────────────────────────────────────────────────────\nHEAP    ████████████████████████████████\nCOORD (p)            ████████████████░░░░░░░░░░░░░░░░▓▓▓▓░░░░░░░░\n                                                      &gt;BUG&lt;</code></div><p>The heap is freed at t2. At t3, the program frees it again. The second free corrupts the allocator&#39;s bookkeeping. The consequences are unpredictable. Crashes, silent corruption, and security vulnerabilities all follow from the same corrupted state.</p><p>Rust prevents this with <span class=\"ea\" data-t=\"p2_14\">single ownership<span class=\"tt\"><span class=\"em-q\">Each value has exactly one owner. The owner frees the value once, automatically.</span></span></span>. One owner, one free, automatic.</p>"},"p1_18":{"t":"Data Race","ctx":"Data (SPACE) race (TIME: parallel) via aliases (COORDINATES).","body":"<div class=\"cb\"><code>TIME  t0             t1                   t2\nEVENTS  create x    p ← addr(x)         ╭─ &gt;write_at_addr(p)&lt;\n                    q ← addr(x)         ╰─ &gt;write_at_addr(q)&lt;\n────────────────────────────────────────────────────────────────────────\nSPACE (x)  ████████████████████████████████▓▓▓▓░░░░░░░░░░░░░░░░░░░░░░░░\nCOORD (p)                ████████████████████████████████████████████\nCOORD (q)                ████████████████████████████████████████████\n                                               &gt;BUG&lt; (parallel writes)</code></div><p>Two coordinates reach the same space, two threads write at the same time, and no synchronization exists between them. The result depends on which write completes first, or whether they interleave at the byte level.</p><p>A data race needs three ingredients. Shared data (SPACE), parallel access (TIME), and at least one writer (COORDINATES). Remove any one and the race disappears.</p><p>This is the <span class=\"ea\" data-t=\"p1_8\">coherence problem<span class=\"tt\"><span class=\"em-q\">The same algebra governs hardware and software.</span></span></span> at the language level. The MESI protocol prevents it in hardware by enforcing the constraint. Many readers or one exclusive writer. The <span class=\"ea\" data-t=\"p2_26\">borrowing rule<span class=\"tt\"><span class=\"em-q\">If any coordinate can write, no other coordinate can exist.</span></span></span> prevents it at compile time with the same constraint. If any coordinate can write, no other coordinate can exist.</p>"},"p1_19":{"t":"Other Memory Bugs","ctx":"Three more bugs complete the taxonomy.","body":"<p>Three more bugs complete the taxonomy.</p><p><strong>Buffer overflow.</strong> Coordinates that exceed the bounds of their space. The buffer has 5 elements. The coordinate points to index 7. The space at that address belongs to something else. Rust prevents this at runtime. Vector and slice indexing checks bounds, and the coordinate cannot exceed the space.</p><p><strong>Uninitialized read.</strong> Read (COORDINATES) before (TIME) the value exists (SPACE). The space is allocated. The coordinate is valid. But TIME has not yet given the space a value. The program reads whatever bits happen to be there. Rust prevents this at compile time. Every variable must be initialized before use.</p><p><strong>Memory leak.</strong> Space outlives all coordinates to it. The coordinate goes out of scope. The allocation remains. No coordinate can reach it, and the program has lost the ability to free it. Rust prevents most leaks with <span class=\"ea\" data-t=\"p2_14\">ownership<span class=\"tt\"><span class=\"em-q\">When the owner goes out of scope, the value is dropped automatically.</span></span></span>. The owner drops the value when its scope ends. Leaks are still possible via <code>mem::forget</code> or reference cycles in <code>Rc</code>, but they require deliberate effort.</p>"},"p1_20":{"t":"Paradigms and Sync Strategies","ctx":"Different languages solve the coherence problem differently. Each strategy adjusts one or more of the three primitives.","body":"<p>Different languages solve the coherence problem differently. Each strategy adjusts one or more of the <span class=\"ea\" data-t=\"p1_13\">three primitives<span class=\"tt\"><span class=\"em-q\">Where data lives. When things happen. How you reach it.</span></span></span>.</p><table><thead><tr><th>Language</th><th>Paradigm</th><th>Coherence Strategy</th></tr></thead><tbody><tr><td>Haskell</td><td>Functional</td><td>No mutation, no problem</td></tr><tr><td>Erlang</td><td>Actor</td><td>Process isolation, message passing</td></tr><tr><td>Clojure</td><td>Functional</td><td>Persistent data structures</td></tr><tr><td>Rust</td><td>Linear/ownership</td><td>Compile-time proof</td></tr><tr><td>Go</td><td>Imperative</td><td>Channels or locks</td></tr><tr><td>Java</td><td>OOP</td><td>Locks, volatile</td></tr><tr><td>C/C++</td><td>Imperative</td><td>Programmer responsibility</td></tr></tbody></table><p>Haskell eliminates the problem. Functions cannot modify state, so nothing gets out of sync. Erlang eliminates sharing. Processes share no memory and communicate by copying messages. Clojure makes mutation structural. Modifying a map returns a new map that shares structure with the old one. Rust proves correctness at compile time with zero runtime cost. Go and Java provide runtime tools and trust the programmer to use them correctly. C and C++ provide no enforcement at all.</p><p>The strategies differ in where they place the cost. Static analysis costs nothing at runtime but rejects some valid programs. Garbage collection accepts all programs but costs runtime performance. Programmer responsibility costs nothing until a bug ships.</p><p>Each language feature targets a <span class=\"ea\" data-t=\"p1_20b\">specific interaction between primitives<span class=\"tt\"><span class=\"em-q\">Which S×T×C interaction does each feature address?</span></span></span>.</p>"},"p1_20b":{"t":"Features as S×T×C Solutions","ctx":"Each language feature targets a specific interaction between the three primitives.","body":"<p>Each language feature targets a specific interaction between primitives.</p><p><strong>SPACE × TIME</strong> — when memory exists.</p><table><thead><tr><th>Feature</th><th>Origin</th><th>Mechanism</th></tr></thead><tbody><tr><td>Garbage collection</td><td>Lisp, 1959</td><td>Runtime tracks reachable memory, reclaims the rest. Eliminates use-after-free and double-free. Cost: unpredictable pauses.</td></tr><tr><td>RAII / destructors</td><td>C++, 1980s</td><td>Memory lifetime tied to scope. Destructor runs when the scope ends. Deterministic, no runtime tracking.</td></tr><tr><td>Reference counting</td><td>~1960s</td><td>Each allocation tracks how many references point to it. Count reaches zero, memory is freed. Cycles are a problem.</td></tr></tbody></table><p><strong>SPACE × COORDINATES</strong> — how many paths lead to memory.</p><table><thead><tr><th>Feature</th><th>Origin</th><th>Mechanism</th></tr></thead><tbody><tr><td><span class=\"ea\" data-t=\"p2_14\">Ownership<span class=\"tt\"><span class=\"em-q\">Each value has one owner. Passing transfers ownership.</span></span></span> / move semantics</td><td>C++11, central to Rust</td><td>Each value has one owner. Passing transfers ownership. Aliasing eliminated by default.</td></tr><tr><td>Value types</td><td>Ancient</td><td>Data copied rather than referenced. Each copy independent. No aliasing.</td></tr><tr><td>Nullable types / Option</td><td>ML, 1970s</td><td>References must be explicitly nullable. Code must handle the null case to compile.</td></tr></tbody></table><p><strong>TIME × COORDINATES</strong> — when a reference is valid.</p><table><thead><tr><th>Feature</th><th>Origin</th><th>Mechanism</th></tr></thead><tbody><tr><td>Lexical scope</td><td>ALGOL, 1958</td><td>References valid only within their declaring scope.</td></tr><tr><td><span class=\"ea\" data-t=\"p2_16\">Lifetime annotations<span class=\"tt\"><span class=\"em-q\">An explicit marker that tells the compiler how long a reference lives.</span></span></span></td><td>Rust, 2010</td><td>Explicit markers for how long references live. No runtime cost.</td></tr><tr><td>Closures</td><td>Lisp, 1958</td><td>Captures force the language to decide: copy, borrow, or move the captured variables.</td></tr></tbody></table><p><strong>SPACE × TIME × COORDINATES</strong> — the full problem.</p><table><thead><tr><th>Feature</th><th>Origin</th><th>Mechanism</th></tr></thead><tbody><tr><td>Mutex / RwLock</td><td>~1960s</td><td>Runtime enforcement of one writer or many readers. Deadlocks possible.</td></tr><tr><td>Atomics</td><td>~1970s</td><td>Hardware-level indivisible operations. Limited to simple operations.</td></tr><tr><td>Channels / message passing</td><td>CSP, 1978</td><td>Transfer ownership between threads. Sender loses access.</td></tr><tr><td>Immutability</td><td>Ancient</td><td>No mutation means no write-read conflicts.</td></tr><tr><td><span class=\"ea\" data-t=\"p2_26\">Rust borrow checker<span class=\"tt\"><span class=\"em-q\">One writer or many readers at compile time. No runtime cost.</span></span></span></td><td>2010</td><td>One writer or many readers at compile time. No runtime cost. The practical form of linear types.</td></tr></tbody></table>"},"p2_1":{"t":"Bindings and Addresses","ctx":"How does the compiler see a dangling reference?","body":"<p>How does the compiler see a <span class=\"ea\" data-t=\"p1_4\">dangling reference<span class=\"tt\"><span class=\"em-q\">Rust rejects it. C++ prints garbage.</span></span></span>?</p><p>Chapter 1 showed a dangling reference that C++ compiles and Rust rejects. The compiler catches it by analyzing what each binding holds and how long it remains valid.</p><div class=\"cb\"><code><span class=\"c\">// C++</span>\n<span class=\"k\">int</span>* r;\n{\n    <span class=\"k\">int</span> x = <span <span class=\"k\">class</span>=\"n\">5</span>;\n    r = &amp;x;\n}\n<span class=\"k\">std</span>::<span class=\"k\">cout</span> &lt;&lt; *r;</code></div><p>Two bindings. The binding <code>x</code> holds a value. The binding <code>r</code> holds an address, the location of <code>x</code>. <code>x</code> is gone after the block ends. <code>r</code> still holds the address and the machine will read whatever bytes sit there.</p><table><thead><tr><th>Name</th><th>Location</th><th>Holds</th></tr></thead><tbody><tr><td><code>x</code></td><td>A</td><td><code>5</code> (a value)</td></tr><tr><td><code>r</code></td><td>B</td><td>address of A (a coordinate)</td></tr></tbody></table><p>The course uses &quot;coordinate&quot; because languages define &quot;pointer&quot; and &quot;reference&quot; differently. <code>T*</code> in C++, <code>&T</code> in Rust, <code>*const T</code> for raw pointers. The conceptual problem is identical. Something tells you where data lives. &quot;Coordinate&quot; abstracts over the language-specific terms.</p><p>Coordinates are not optional. Copying large data costs time and energy, so programs pass addresses instead of values. Graphs, trees, and linked lists require indirection because you cannot inline a cycle. Any language that cares about performance or expressiveness needs coordinates.</p><p>The coordinate <code>r</code> is syntactically independent. It has its own declaration, its own location, its own scope. It is semantically dependent. Its purpose is to refer to <code>x</code>, and without <code>x</code>, the address in <code>r</code> points to nothing meaningful. These lifetimes falling out of sync produces a coherence failure.</p>"},"p2_2":{"t":"How Programs Manage Space","ctx":"Bindings hold values or coordinates. Where does the space live?","body":"<p>Bindings hold values or coordinates. Where does the space live?</p><p>The dangling reference exploits a gap between two levels. At the language level, <code>x</code>&#39;s space is gone when the scope ends. At the machine level, the bytes are still there. Understanding this gap requires knowing how programs manage memory.</p><p><strong>Stack.</strong> The compiler manages the stack. It determines at compile time how much space each function needs and lays out a stack frame for each call. The compiler considers those slots available for reuse at the end of each scope. The machine does not zero the bytes and does not reclaim them. It adjusts the stack pointer. A coordinate that still holds the old address can read the bytes, and the machine will comply. The language calls this undefined behavior. The machine calls it a normal read.</p><p><strong>Heap.</strong> The runtime manages the heap. An explicit allocation (<code>malloc</code>, <code>new</code>, <code>Box::new</code>) reserves bytes and returns a coordinate. An explicit deallocation (<code>free</code>, <code>delete</code>, dropping the owner) marks them reusable. The same gap applies. After deallocation, a coordinate that still holds the address can read stale bytes. The language calls this a <span class=\"ea\" data-t=\"p1_15\">use-after-free<span class=\"tt\"><span class=\"em-q\">A heap pointer followed after the heap is freed.</span></span></span>. The machine calls it a normal read.</p><p>The heap adds a problem the stack does not have. The compiler can see the scope boundaries that tie stack space to a known lifetime. Any code holding a heap coordinate can free the space, and heap coordinates can travel into other variables, function arguments, and data structures. No lexical structure in the source text tells the compiler when heap space will die.</p><div class=\"cb\"><code>STACK (tree)                 HEAP + STACK (hybrid)\nmain ─────────┐              main\n├── foo       │              ├── foo ──────────┐\n│   └── bar   │              │   └── bar       │\n│       └── baz│                               ▼\n└── qux       │              └── qux     [heap]\n              │                           ▲\n              │                           │\nCoordinates point toward     Heap coordinates escape\nenclosing scopes.            the scope tree.\nReclamation at scope end.    Reclamation: anywhere.\nCompiler sees both.          Compiler sees neither.</code></div><p>The rest of this chapter uses &quot;reclaimed&quot; to mean the language no longer considers the space as belonging to any live binding. Stack space is reclaimed when its scope ends. Heap space is reclaimed when something deallocates it. <span class=\"ea\" data-t=\"p2_14\">Ownership<span class=\"tt\"><span class=\"em-q\">Heap lifetime follows owner scope.</span></span></span> ties heap reclamation to scope boundaries, making heap behave like stack from the compiler&#39;s perspective.</p>"},"p2_3":{"t":"What the Compiler Needs to See","ctx":"Source text has hierarchical structure. Coordinates do not. The analysis needs both.","body":"<p>Stack and heap manage space differently. What does the compiler need to catch bugs across both?</p><p>Source text has hierarchical structure. Functions contain blocks, blocks contain statements, statements contain expressions. A variable declared inside a block is visible only within that block and any nested blocks. Names respect the scope hierarchy, but coordinates do not.</p><div class=\"cb\"><code><span class=\"k\">fn</span> <span class=\"m\">first</span>(list: &amp;[<span class=\"m\">i32</span>]) -&gt; &amp;<span class=\"m\">i32</span> {\n    &amp;list[<span class=\"n\">0</span>]\n}\n<span class=\"k\">fn</span> <span class=\"m\">main</span>() {\n    <span class=\"k\">let</span> v = <span class=\"m\">vec!</span>[<span class=\"n\">1</span>, <span class=\"n\">2</span>, <span class=\"n\">3</span>];\n    <span class=\"k\">let</span> r = first(&amp;v);\n    <span class=\"m\">println!</span>(<span class=\"s\">\"{}\"</span>, r);\n}</code></div><p>The scope hierarchy sees <code>v</code> and <code>r</code> in <code>main</code>&#39;s scope, and <code>list</code> in <code>first</code>&#39;s scope. Three names, three declarations, three scope boundaries. The scope hierarchy does not see that <code>r</code> holds a coordinate into <code>v</code>&#39;s space. The coordinate traveled from <code>main</code> into <code>first</code> as the argument <code>list</code>, reached into the vector&#39;s first element, and traveled back as the return value bound to <code>r</code>. The connection between <code>r</code> and <code>v</code> crosses two scope boundaries and routes across a function body the caller cannot see.</p><p>The analysis needs two things at every point in the program. Where does each coordinate point? When is each piece of space reclaimed? The compiler can answer the first question when every coordinate has a known origin. It can answer the second for stack space, because scope boundaries are visible. It cannot answer it for heap space, because heap deallocation has no lexical structure. <span class=\"ea\" data-t=\"p2_16\">Lifetime annotations<span class=\"tt\"><span class=\"em-q\">Declaring which input a borrowed output connects to.</span></span></span> at function signatures bridge this gap by declaring which input a borrowed output connects to.</p><p>The scope hierarchy tracks names and boundaries. The analysis needs data flow and reclamation across boundaries. RAM has no scope structure at all. Memory is a sequence of numbered cells, and any address can read or write any cell. The compiler bridges these worlds by building a <strong>control flow graph</strong>. Nodes are statements. Edges connect statements where execution can flow from one to the next. Branches create multiple outgoing edges. Loops create backward edges.</p>"},"p2_4":{"t":"Detecting Access to Reclaimed Space","ctx":"The compiler has a control flow graph. How does it find the bug?","body":"<p>The compiler analyzes the control flow graph by tracking coordinates along every path. It records where each coordinate is created, what space it points to, and when that space is reclaimed. A path connecting a reclamation point to an access point for the same space means the program can reach reclaimed memory. The compiler rejects when any such path exists.</p><p>The <span class=\"ea\" data-t=\"p1_4\">dangling reference<span class=\"tt\"><span class=\"em-q\">Rust rejects it at compile time.</span></span></span> from Chapter 1, traced on the graph:</p><div class=\"cb\"><code><span class=\"k\">fn</span> <span class=\"m\">main</span>() {\n    <span class=\"k\">let</span> r;\n    {\n        <span class=\"k\">let</span> x = <span class=\"n\">5</span>;      <span class=\"c\">// A</span>\n        r = &amp;x;          <span class=\"c\">// B</span>\n    }                     <span class=\"c\">// C</span>\n    <span class=\"m\">println!</span>(<span class=\"s\">\"{}\"</span>, r);   <span class=\"c\">// D</span>\n}\n\n    ┌───┐\n    │ A │  <span class=\"k\">let</span> x = <span class=\"n\">5</span>\n    └─┬─┘\n      ▼\n    ┌───┐\n    │ B │  r = &amp;x\n    └─┬─┘\n      ▼\n    ┌───┐\n    │ C │  x's space reclaimed (scope ends)\n    └─┬─┘\n      ▼\n    ┌───┐\n    │ D │  *r  ACCESS with reclaimed space\n    └───┘</code></div><p>One path. C reclaims the space. D accesses it through <code>r</code>. The path from C to D exists. The compiler rejects.</p><p>A more complex example with branching shows why the compiler must check all paths:</p><div class=\"cb\"><code><span class=\"k\">fn</span> <span class=\"m\">example</span>(flag: <span class=\"m\">bool</span>) {\n    <span class=\"k\">let</span> r;\n    <span class=\"k\">if</span> flag {\n        <span class=\"k\">let</span> x = <span class=\"n\">5</span>;                <span class=\"c\">// B</span>\n        r = &amp;x;\n        <span class=\"m\">println!</span>(<span class=\"s\">\"{}\"</span>, *r);       <span class=\"c\">// D: safe, x alive</span>\n    } <span class=\"k\">else</span> {\n        <span class=\"k\">let</span> y = <span class=\"n\">10</span>;               <span class=\"c\">// C</span>\n        r = &amp;y;\n    }                             <span class=\"c\">// y's space reclaimed</span>\n    <span class=\"m\">println!</span>(<span class=\"s\">\"{}\"</span>, *r);           <span class=\"c\">// G: unsafe if else taken</span>\n}\n\n    ┌───┐\n    │ A │  <span class=\"k\">if</span> flag\n    └─┬─┘\n  ┌───┴────┐\n  ▼        ▼\n┌───┐   ┌───┐\n│ B │   │ C │\n│x=<span class=\"n\">5</span>│   │y=<span class=\"n\">10</span>│\n│r=&amp;x│  │r=&amp;y│\n└─┬─┘   │ ✗ │  y reclaimed\n  │     └─┬─┘\n  ▼       ▼\n┌───┐   ┌───┐\n│ D │   │ F │\n│*r │   │ ✗ │  propagated\n│safe│  └─┬─┘\n└─┬─┘     │\n  └───┬───┘\n      ▼\n    ┌───┐\n    │ G │\n    │*r │  <span class=\"k\">unsafe</span> on <span class=\"k\">else</span> path ✗\n    └───┘</code></div><p>The access at D is safe because no reclamation reaches it along any path. The access at G is unsafe because a path from C reaches it through F. The compiler finds the unsafe path and rejects. It does not matter that the if-branch is safe. One unsafe path is enough.</p><p>The compiler checks all paths because it cannot know at compile time which branch the program will take at runtime. This is a <span class=\"ea\" data-t=\"p2_11\">sound<span class=\"tt\"><span class=\"em-q\">Never accept unsafe code.</span></span></span> analysis. It may reject programs that would actually be safe, but it will never accept a program that can reach reclaimed memory.</p>"},"p2_5":{"t":"Why C++ Compilers Skip This Analysis","ctx":"The analysis exists. Most compilers don't attempt it. Three properties of C and C++ prevent it.","body":"<p>Three properties of C and C++ prevent building the complete control flow graph the analysis requires.</p><p><strong>Separate compilation.</strong> Each source file becomes an object file independently. A call to a function defined in another file appears as an external symbol, resolved by the linker after analysis is over. The graph has edges leading to functions whose bodies are invisible.</p><p><strong>Function pointers.</strong> The call target is a runtime value. The compiler cannot determine which function executes. The graph has edges to unknown destinations.</p><p><strong>Unconstrained pointers.</strong> Even with a complete graph, the compiler needs to track where every coordinate points. C++ allows coordinates to be created without naming a target.</p><div class=\"cb\"><code><span class=\"k\">int</span>* p = (<span class=\"k\">int</span>*)0x1234;     <span class=\"c\">// coordinate from integer</span>\n<span class=\"k\">int</span>* q = p + <span <span class=\"k\">class</span>=\"n\">5</span>;            <span class=\"c\">// coordinate from arithmetic</span>\n<span class=\"k\">void</span>* v = p;               <span class=\"c\">// type erased</span>\nuintptr_t n = (uintptr_t)p; <span class=\"c\">// stored as integer</span></code></div><p>An integer cast to a pointer, pointer arithmetic, <code>void*</code> type erasure. The analysis requires knowing where every coordinate points. C++ provides no such guarantee. Rust <span class=\"ea\" data-t=\"p2_13\">constrains coordinate creation<span class=\"tt\"><span class=\"em-q\">Every reference has a known target.</span></span></span> to fix this.</p><p>These are three specific obstacles. They generalize to <span class=\"ea\" data-t=\"p2_6\">two deeper problems<span class=\"tt\"><span class=\"em-q\">Flat RAM and cross-scope data flow.</span></span></span> that any language must address.</p>"},"p2_6":{"t":"RAM Is Flat","ctx":"One of two deeper problems behind C++'s obstacles.","body":"<p>The machine model has no scope hierarchy. Memory is a sequence of numbered cells. A pointer is a number. Any address can read or write any cell. Nothing in the machine prevents code from reading a cell after the scope that allocated it has ended. Nothing prevents fabricating an address the compiler cannot trace.</p><p>C++&#39;s unconstrained pointers are a symptom of this deeper problem. The machine provides no structure for the compiler to exploit. Coordinates can arise from anywhere and point to anything.</p><p>A compiler that wants to answer &quot;where does each coordinate point?&quot; needs the language itself to <span class=\"ea\" data-t=\"p2_13\">constrain how coordinates are created<span class=\"tt\"><span class=\"em-q\">Every reference has a known target.</span></span></span>. The analysis cannot work if coordinates appear from nowhere.</p><p>The second deeper problem is <span class=\"ea\" data-t=\"p2_7\">data that crosses scope boundaries<span class=\"tt\"><span class=\"em-q\">The scope tree tracks names, not data flow.</span></span></span>.</p>"},"p2_7":{"t":"Data Flow Crosses Scope Boundaries","ctx":"Scopes track names. Data flows between them.","body":"<p>The scope tree tracks where names live. It does not track where data flows between them. A function argument carries a coordinate into a nested scope. A return value carries one out. An assignment can carry a coordinate to a binding with a different lifetime.</p><p>A function&#39;s caller passes <code>&v</code> in and receives a coordinate out. The caller cannot determine whether the returned coordinate still points into <code>v</code>&#39;s space. The signature says nothing about the connection. Separate compilation means the compiler cannot see the function body to find out.</p><p>C++&#39;s separate compilation and function pointers are symptoms. The deeper problem is that data flow between scopes is invisible to the scope tree. A compiler that wants to trace coordinates across function boundaries needs the language to <span class=\"ea\" data-t=\"p2_16\">declare how data flows between scopes<span class=\"tt\"><span class=\"em-q\">Lifetime annotations declare which input a borrowed output connects to.</span></span></span>.</p><p>One more obstacle remains. <span class=\"ea\" data-t=\"p2_8\">Types that contain pointers<span class=\"tt\"><span class=\"em-q\">Byte-copying a String duplicates the coordinate without duplicating the space.</span></span></span> hide coordinates inside their stack representation.</p>"},"p2_8":{"t":"Invisible Coordinate Copying","ctx":"Some types hide coordinates inside their stack representation.","body":"<p>A <code>String</code>&#39;s stack representation holds a heap pointer, a length, and a capacity. The heap pointer is a coordinate to managed space. Byte-copying the stack representation duplicates that coordinate without duplicating the heap buffer. Two bindings now hold coordinates to the same buffer, and both believe they own it.</p><div class=\"cb\"><code>Byte-copy on i32:          Byte-copy on String (hypothetical):\n\n+----------+               +----------+       +----------+\n|    5     | &lt;- x (owner)  | ptr -----|---===&gt;| \"hello\"  |\n+----------+               +----------+  |    |          |\n|    5     | &lt;- y (owner)  | ptr -----|--+    +----------+\n+----------+               +----------+\n\nIndependent space.          Two owners, one space.</code></div><p>An <code>i32</code> has no heap pointer and no cleanup obligation. Duplicating its bytes produces two independent values. A <code>String</code> manages heap space, and byte-copying it duplicates the coordinate without duplicating the space itself. The analysis tracks one owner per allocation. Invisible duplication defeats that tracking by creating a second owner the compiler did not authorize.</p><p>The compiler needs to know which types are safe to byte-copy and which are not. Rust <span class=\"ea\" data-t=\"p2_19\">prevents accidental byte-copying<span class=\"tt\"><span class=\"em-q\">Why does Rust move by default instead of copying?</span></span></span> of types that manage internal space.</p><p><span class=\"ea\" data-t=\"p2_8b\">Other languages<span class=\"tt\"><span class=\"em-q\">Five strategies, from doing nothing to compile-time proofs.</span></span></span> approach these obstacles in entirely different ways.</p>"},"p2_8b":{"t":"How Other Languages Approach These Obstacles","ctx":"Five strategies, from doing nothing to doing everything at compile time.","body":"<table><thead><tr><th>Strategy</th><th>Languages</th><th>Tradeoff</th></tr></thead><tbody><tr><td>No analysis</td><td>C, C++</td><td>Fast, unsafe. Programmer responsibility.</td></tr><tr><td>Static analyzers</td><td>Clang SA, Coverity, Infer</td><td>Separate pass, expensive, heuristic. Misses real bugs, reports false positives.</td></tr><tr><td>Region annotations</td><td>Cyclone, ATS</td><td>Compile-time safety. High annotation burden, never widely adopted.</td></tr><tr><td>Garbage collection</td><td>Java, Go, Python, C#</td><td>Space lives until no coordinates reach it. Runtime cost: GC pauses, memory overhead.</td></tr><tr><td>Ownership</td><td>Rust</td><td>Compile-time proof. Zero runtime cost. Requires restructuring when the approximation rejects.</td></tr></tbody></table><p><strong>No analysis.</strong> C and C++ compilers do not attempt systematic tracking. Some warn about obvious cases like returning addresses of locals, but the obstacles described in this chapter prevent anything more.</p><p><strong>Static analyzers.</strong> Tools like Clang Static Analyzer, Coverity, and Infer run separately from compilation. They use abstract interpretation and symbolic execution to trace paths. The analysis is expensive and trades precision for scalability. Heuristics reduce false positives, which means real bugs are missed too.</p><p><strong>Region annotations.</strong> Cyclone added region types to C. Every pointer carries a region annotation, and the compiler tracks region lifetimes. ATS uses dependent and linear types. Both achieve compile-time safety, but annotation requirements and limited C compatibility kept them in research settings.</p><p><strong>Garbage collection.</strong> The problem is coordinates pointing to space that no longer exists. GC inverts the relationship. Space is not freed until no coordinates point to it. The runtime traces from roots to find reachable objects. The tradeoff is runtime overhead in memory for bookkeeping, CPU for tracing, and unpredictable pauses.</p><p><strong>Ownership.</strong> Rust patches each obstacle so the compiler can build the complete graph and track every coordinate. Each value has one owner, transfer invalidates the previous binding, and the runtime cost is zero. But the analysis faces a <span class=\"ea\" data-t=\"p2_10\">fundamental limit<span class=\"tt\"><span class=\"em-q\">Not just hard. Mathematically impossible.</span></span></span> that no language design can remove.</p><p>Each <span class=\"ea\" data-t=\"p1_20\">paradigm<span class=\"tt\"><span class=\"em-q\">Seven languages, seven strategies.</span></span></span> represents a different point in the design space.</p>"},"p2_9":{"t":"Where Branches Break the Analysis","ctx":"Even with every obstacle patched, one problem remains.","body":"<p>Branches create paths that may or may not execute, depending on runtime values.</p><div class=\"cb\"><code>r = uninitialized\n{\n    x = 5\n    if some_condition(n) {\n        r = &amp;x\n    }\n}\n// x is gone here\nif some_condition(n) {\n    print(*r) // r used after x's space is reclaimed?\n}</code></div><p>Whether <code>*r</code> accesses reclaimed space depends on whether <code>some_condition(n)</code> returns true. The compiler would need to evaluate it. That evaluation depends on <code>n</code>, and <code>some_condition</code> might contain arbitrary computation. The <span class=\"ea\" data-t=\"p2_4\">analysis checks all paths<span class=\"tt\"><span class=\"em-q\">The compiler rejects when any path is unsafe.</span></span></span> and rejects when any path is unsafe.</p><div class=\"cb\"><code>// Collatz sequence: does this terminate for all inputs?\nn = input\nwhile n != 1 {\n    if n is even { n = n / 2 }\n    else { n = 3*n + 1 }\n}</code></div><p>Whether the Collatz sequence terminates for all inputs remains unknown. Mathematicians have tested every number up to 10²⁰. No proof exists. A <code>some_condition(n)</code> that checks Collatz termination would force the compiler to solve an open problem in mathematics.</p><p>Arbitrary computation resists static prediction, and <span class=\"ea\" data-t=\"p2_10\">no algorithm can avoid this limitation<span class=\"tt\"><span class=\"em-q\">Turing proved it in 1936.</span></span></span>.</p>"},"p2_10":{"t":"Why Perfect Analysis Is Impossible","ctx":"Not just hard. Mathematically impossible.","body":"<p><strong>The Halting Problem (Turing, 1936).</strong> A termination checker must return an answer for any program it examines. The program being examined has no such obligation. It can run forever, halt immediately, or do anything in between.</p><p>A program can read the checker&#39;s source code, call the checker on itself, and then do the opposite of what the checker predicts. If the checker predicts &quot;terminates,&quot; the program loops forever. If the checker predicts &quot;loops forever,&quot; the program terminates. The checker is wrong regardless of what it answers. No algorithm can avoid this contradiction.</p><p><strong>Rice&#39;s Theorem (1951)</strong> generalizes the result. Any non-trivial semantic property of programs is undecidable. &quot;Non-trivial&quot; means some programs have the property and some do not. &quot;Semantic&quot; means the property depends on what the program does, not how it is written.</p><p>&quot;Does this COORDINATE reach reclaimed SPACE?&quot; is such a property. So is &quot;does this program have a data race?&quot; and &quot;does this program dereference a dangling pointer?&quot; These are mathematical impossibilities that apply to any static analysis tool, in any language, built by any team.</p><p>The compiler cannot be perfect and must <span class=\"ea\" data-t=\"p2_11\">choose between two failures<span class=\"tt\"><span class=\"em-q\">Sound or complete.</span></span></span>. <span class=\"ea\" data-t=\"p3_4\">Escape hatches<span class=\"tt\"><span class=\"em-q\">Restructure, encode in types, runtime checks, or unsafe.</span></span></span> exist for valid code the compiler rejects.</p>"},"p2_11":{"t":"Sound or Complete","ctx":"The compiler must choose which failure to accept.","body":"<p><strong>Sound</strong> means the compiler never accepts unsafe code. It may reject code that is actually safe.</p><p><strong>Complete</strong> means the compiler never rejects safe code. It may accept code that is actually unsafe.</p><p>No compile-time analysis can be both sound and complete for questions about program behavior. Accepting unsafe code means security vulnerabilities and undefined behavior. Rejecting safe code means restructuring or using escape hatches. The asymmetry between failure modes determines the choice.</p><p>Rust chooses soundness. The <span class=\"ea\" data-t=\"p1_8b\">readers-writers rule<span class=\"tt\"><span class=\"em-q\">The compiler has no runtime. The readers-writers rule is the only coherence policy enforceable without one.</span></span></span> is the only coherence policy a compiler can enforce without a runtime. The <span class=\"ea\" data-t=\"p2_26\">approximation<span class=\"tt\"><span class=\"em-q\">The borrowing rule.</span></span></span> rejects some valid programs, and that is the acceptable cost.</p>"},"p2_12":{"t":"Names Are Exact, Data Flow Is Approximated","ctx":"The boundary between what the compiler knows precisely and what it approximates.","body":"<p>The compiler handles names exactly. Scope boundaries, variable declarations, and field names are all visible in source text. The scope tree captures where every name lives and when every name expires. This part of the analysis is decidable and precise.</p><p>The approximation falls entirely on data flow. Which path a branch takes, which method a function pointer calls, whether two indices refer to the same element. These depend on runtime values, and Rice&#39;s theorem says no algorithm answers correctly for all inputs. The compiler assumes the worst case for every question about data flow.</p><p>Four obstacles face the analysis. <span class=\"ea\" data-t=\"p2_6\">Flat RAM<span class=\"tt\"><span class=\"em-q\">The machine provides no structure for the compiler.</span></span></span>, <span class=\"ea\" data-t=\"p2_7\">cross-scope data flow<span class=\"tt\"><span class=\"em-q\">The scope tree tracks names, not data flow.</span></span></span>, <span class=\"ea\" data-t=\"p2_8\">invisible duplication<span class=\"tt\"><span class=\"em-q\">Byte-copying duplicates coordinates.</span></span></span>, and <span class=\"ea\" data-t=\"p2_10\">undecidability<span class=\"tt\"><span class=\"em-q\">Turing proved it in 1936.</span></span></span>. One fundamental constraint governs the response. The compiler chooses soundness over completeness. Rust patches each obstacle with a specific design choice.</p>"},"p2_13":{"t":"Constrained Coordinates","ctx":"RAM is flat. Pointers are numbers. How does Rust fix this?","body":"<p>In C++, a pointer is a number that happens to serve as an address. The language provides operations on numbers, and pointers inherit them. You can cast an integer to a pointer, perform arithmetic to compute new addresses, erase type information through <code>void*</code>, and store pointers in integer variables. Each operation severs the connection between the coordinate and its origin. The compiler cannot trace where the pointer points.</p><p>Rust references are not numbers. A reference is a typed relationship between a binding and a target. The language provides one way to create a reference. The <code>&</code> operator applied to an existing value. There is no syntax for creating a reference from an integer. There is no reference arithmetic. There is no type erasure for references.</p><div class=\"cb\"><code><span class=\"k\">let</span> x = <span class=\"n\">5</span>;\n<span class=\"k\">let</span> r = &amp;x;     <span class=\"c\">// r points to x, compiler knows this</span></code></div><p>When the compiler sees <code>&x</code>, it records that the resulting reference points to <code>x</code>. Every reference in the program has a known origin. The compiler can trace any reference back to the space it refers to.</p><table><thead><tr><th>C++ allows</th><th>Rust equivalent</th><th>Why it matters</th></tr></thead><tbody><tr><td><code>(int*)0x1234</code></td><td>Does not exist</td><td>Every reference has a known target</td></tr><tr><td><code>p + 5</code></td><td>Does not exist</td><td>References cannot drift to unknown locations</td></tr><tr><td><code>void* v = p</code></td><td>Does not exist for references</td><td>Type information preserved</td></tr><tr><td><code>(uintptr_t)p</code></td><td>Does not exist for references</td><td>References cannot become integers</td></tr></tbody></table><p>Raw pointers in Rust (<code>*const T</code>, <code>*mut T</code>) allow some of these operations, but dereferencing a raw pointer requires <code>unsafe</code> code. The borrow checker analyzes safe Rust, where all coordinates are references with known origins. This patches the obstacle that <span class=\"ea\" data-t=\"p2_6\">RAM is flat<span class=\"tt\"><span class=\"em-q\">The machine provides no structure.</span></span></span>. Constrained references ensure every coordinate has a known origin on the graph.</p>"},"p2_14":{"t":"Ownership","ctx":"Heap space has no lexical structure. How does the compiler know when it dies?","body":"<p>Stack space is reclaimed when scopes end, and the compiler sees scope boundaries in the source text. Heap space in C++ has unpredictable lifetimes. A <code>new</code> expression allocates, and that space lives until some code calls <code>delete</code>. The coordinate to that space can be copied, passed to functions, and stored in data structures. Any code holding the coordinate can free the space. Any code holding a copy can use it afterward and find the space gone. Nothing in the source text tells the compiler when heap space will die.</p><p>Rust makes heap lifetimes predictable by assigning exactly one owner to each allocation. The owner is a binding, and that binding has a scope. Rust deallocates the owned space when the owner&#39;s scope ends. Heap lifetime follows owner scope, the same way stack lifetime follows function scope.</p><div class=\"cb\"><code><span class=\"k\">fn</span> <span class=\"m\">example</span>() {\n    <span class=\"k\">let</span> v = <span class=\"m\">vec!</span>[<span class=\"n\">1</span>, <span class=\"n\">2</span>, <span class=\"n\">3</span>];   <span class=\"c\">// v owns heap space</span>\n    <span class=\"c\">// ... use v ...</span>\n}   <span class=\"c\">// v's scope ends, heap space freed</span></code></div><p>Ownership can transfer from one binding to another. This is a move. After the move, the original binding is no longer valid, and the new binding is the owner.</p><div class=\"cb\"><code><span class=\"k\">fn</span> <span class=\"m\">example</span>() {\n    <span class=\"k\">let</span> v = <span class=\"m\">vec!</span>[<span class=\"n\">1</span>, <span class=\"n\">2</span>, <span class=\"n\">3</span>];   <span class=\"c\">// v owns</span>\n    <span class=\"k\">let</span> w = v;                <span class=\"c\">// ownership moves to w, v now invalid</span>\n    <span class=\"c\">// ... use w ...</span>\n}   <span class=\"c\">// w's scope ends, heap space freed</span></code></div><p>At any moment, exactly one binding owns each allocation. The compiler tracks ownership through assignments and function calls. This transforms the analysis. Without ownership, heap space can be reclaimed at any point where code calls <code>free</code> or <code>delete</code>. Ownership ties heap reclamation to scope boundaries, which the compiler already sees. Heap behaves like stack for the purposes of lifetime analysis.</p>"},"p2_15":{"t":"The Notation","ctx":"Rust syntax hides which binding is owner vs alias. The notation makes every operation explicit.","body":"<p>Rust&#39;s assignment syntax hides which kind of binding a <code>let</code> produces. <code>let result = create()</code> and <code>let elem = first(&list)</code> look identical. One produces an owner. The other produces an alias. The language hides these distinctions deliberately. A learner building the model for the first time does not have the experience to recognize what the syntax conceals.</p><p>The course includes a companion crate called <code>notation</code> (coming soon) with a macro called <code>explicit!</code>. Inside the macro, every operation declares what it does.</p><div class=\"cb\"><code><span class=\"m\">explicit!</span> {\n    <span class=\"k\">let</span> owner(s) = take(<span class=\"m\">String</span>::from(<span class=\"s\">\"hello\"</span>));  <span class=\"c\">// s takes ownership</span>\n    <span class=\"k\">let</span> owner(t) = take(s);                       <span class=\"c\">// t takes from s, s invalid</span>\n    <span class=\"k\">let</span> alias(r) = coord_exclusive(t);            <span class=\"c\">// r is a coordinate to t's space</span>\n    <span class=\"m\">println!</span>(<span class=\"s\">\"{}\"</span>, at(r));                         <span class=\"c\">// follow the coordinate</span>\n}</code></div><p>The left side of each <code>let</code> declares the kind of binding. <code>owner</code> means the binding controls when space is reclaimed. <code>alias</code> means the binding holds a coordinate to space owned elsewhere. The right side declares what happens. <code>take</code> means ownership transfers. An existing binding passed as the argument becomes invalid. <code>coord_exclusive</code> creates an <span class=\"ea\" data-t=\"p2_21\">exclusive coordinate<span class=\"tt\"><span class=\"em-q\">One coordinate, read and write access.</span></span></span>. <code>at</code> follows a coordinate to its target.</p><p>The vocabulary maps to the framework from the earlier chapter. <code>owner</code> tracks SPACE × TIME. <code>alias</code> tracks COORDINATES. <code>take</code> is an ownership transfer in TIME. <code>coord_exclusive</code> creates a COORDINATE with exclusive access. Coordinates come in two kinds. <code>coord_exclusive</code> grants read and write access. <code>coord_shared</code> grants read access while allowing multiple coordinates to coexist.</p>"},"p2_16":{"t":"Lifetime Annotations","ctx":"Data flow crosses scope boundaries. Lifetime annotations declare the connections the compiler cannot infer.","body":"<p>Data flow crosses scope boundaries. Constrained coordinates and ownership make both questions answerable within a single function. Separate compilation limits this. The compiler processes each function independently. A caller cannot see the body of a function that returns a reference and cannot determine where the reference points.</p><p>Function signatures solve this. A borrowed output returns a reference that points into space owned by one of the inputs. The <code>'a</code> label links an output to its input.</p><div class=\"cb\"><code><span class=\"k\">fn</span> <span class=\"m\">first</span>&lt;'a&gt;(list: &amp;'a [<span class=\"m\">i32</span>]) -&gt; &amp;'a <span class=\"m\">i32</span> {\n    &amp;list[<span class=\"n\">0</span>]\n}\n\n<span class=\"k\">fn</span> <span class=\"m\">search</span>&lt;'a, 'b&gt;(map: &amp;'a Map, key: &amp;'b K) -&gt; &amp;'a V\n<span class=\"c\">//                                               output borrows from map, not key</span>\n\n<span class=\"k\">fn</span> <span class=\"m\">either</span>&lt;'a&gt;(a: &amp;'a <span class=\"m\">i32</span>, b: &amp;'a <span class=\"m\">i32</span>, flag: <span class=\"m\">bool</span>) -&gt; &amp;'a <span class=\"m\">i32</span>\n<span class=\"c\">//                                                    output borrows from both</span></code></div><p>The compiler verifies both directions. Given the function body, it checks that the implementation returns a reference derived from the declared input. Given the call site, it checks that the caller keeps the input alive while using the output. Function pointers, trait objects, and cross-crate calls all work from the signature alone.</p><p>The same annotation applies to structs. A struct can hold a coordinate to space owned by a different binding. That coordinate is derived data, and the struct depends on the external space staying alive. The <code>'a</code> on the struct declares this dependency.</p><div class=\"cb\"><code><span class=\"k\">struct</span> Viewer&lt;'a&gt; {\n    data: &amp;'a [<span class=\"m\">i32</span>],\n}</code></div><p>The compiler uses <code>'a</code> to verify that any code creating a <code>Viewer</code> keeps the referenced space alive long enough.</p><div class=\"cb\"><code><span class=\"k\">fn</span> <span class=\"m\">main</span>() {\n    <span class=\"k\">let</span> viewer;\n    {\n        <span class=\"k\">let</span> v = <span class=\"m\">vec!</span>[<span class=\"n\">1</span>, <span class=\"n\">2</span>, <span class=\"n\">3</span>];\n        viewer = Viewer { data: &amp;v };\n    } <span class=\"c\">// v's space reclaimed here</span>\n    <span class=\"m\">println!</span>(<span class=\"s\">\"{:?}\"</span>, viewer.data); <span class=\"c\">// REJECTED: viewer outlives v</span>\n}</code></div><div class=\"er\"><strong>error[E0597]:</strong> <code>v</code> does not live long enough</div><p>Function lifetime annotations declare that an output holds a coordinate to an input&#39;s space. Struct lifetime annotations declare that the struct holds a coordinate to external space. Both tell the compiler which space a coordinate depends on, and the mechanism is the same.</p>"},"p2_17":{"t":"Desugaring and Lifetime Elision","ctx":"How do operators like `v[0]` connect to lifetimes?","body":"<p>Rust desugars operator syntax to trait method calls. <code>v[0]</code> desugars to <code>*v.index(0)</code>, and <code>Index::index</code> has this signature:</p><div class=\"cb\"><code><span class=\"k\">fn</span> <span class=\"m\">index</span>(&amp;<span class=\"k\">self</span>, index: <span class=\"m\">usize</span>) -&gt; &amp;<span class=\"k\">Self</span>::Output\n<span class=\"c\">// With lifetime written out:</span>\n<span class=\"k\">fn</span> <span class=\"m\">index</span>&lt;'a&gt;(&amp;'a <span class=\"k\">self</span>, index: <span class=\"m\">usize</span>) -&gt; &amp;'a <span class=\"k\">Self</span>::Output</code></div><p>The function takes a <code>&Vec<i32></code> and returns a <code>&i32</code>. One coordinate goes in, one coordinate comes out. The shared <code>'a</code> declares that the returned element coordinate is valid only while the input Vec coordinate is valid. The compiler reads this temporal dependency from the signature and enforces it at every call site.</p><p>Rust calls this inference <em>lifetime elision</em>. A function with one reference input and one reference output infers the connection. A method on <code>&self</code> or <code>&mut self</code> infers the output borrows from <code>self</code>. The programmer writes explicit <code>'a</code> labels only when the inference is ambiguous. Multiple reference inputs, no <code>self</code>, or an output that needs to declare which specific input it borrows from.</p>"},"p2_18":{"t":"Using Data, Managing Space","ctx":"An owner controls when space is reclaimed. An alias holds a coordinate to space owned elsewhere. These are different operations.","body":"<p>An <code>owner</code> controls when space is reclaimed. An <code>alias</code> holds a coordinate to space owned elsewhere. Using the data and managing the space are different operations, and Rust enforces the difference.</p><p>A coordinate, even an exclusive one, lets code read and write the target data. It cannot free the space, move ownership out, or replace the allocation. Only the <code>owner</code> can manage the space.</p><div class=\"cb\"><code><span class=\"m\">explicit!</span> {\n    <span class=\"k\">let</span> owner(b) = take(<span class=\"m\">Box</span>::new(<span class=\"m\">String</span>::from(<span class=\"s\">\"hello\"</span>)));  <span class=\"c\">// b owns heap space</span>\n    <span class=\"k\">let</span> alias(r) = coord_exclusive(b);                     <span class=\"c\">// r can read and write</span>\n    <span class=\"c\">// take(at(r)) would be invalid: r is an alias, aliases cannot own</span>\n}</code></div><p><code>take(b)</code> works because <code>b</code> is an <code>owner</code>. The compiler forbids <code>take</code> when the argument is an <code>alias</code>. A function that receives an <code>alias</code> parameter cannot call <code>take</code> on it, cannot destroy the space, cannot move ownership away. The caller knows that after the function returns, the space still exists. The function accessed the data. The <code>owner</code> kept control of the space.</p><table><thead><tr><th>Operation</th><th>C++ via <code>unique_ptr<string>&</code></th><th>Rust via <code>alias(r) = coord_exclusive(b)</code></th></tr></thead><tbody><tr><td>Read value</td><td><code>*ref</code></td><td><code>at(at(r))</code></td></tr><tr><td>Write value</td><td><code>*ref = v</code></td><td><code>at(at(r)) = v</code></td></tr><tr><td>Call methods</td><td><code>ref->method()</code></td><td><code>at(r).method()</code></td></tr><tr><td>Move out</td><td><code>auto s = move(*ref)</code></td><td><code>take(at(r))</code> <strong>forbidden</strong></td></tr><tr><td>Destroy space</td><td><code>ref.reset()</code></td><td><strong>forbidden</strong></td></tr></tbody></table><p>The first three rows use data. The last two manage space. C++ allows a coordinate to manage the space. Rust does not. This distinction makes the compiler&#39;s analysis airtight. Signatures that declare <code>alias</code> connections are telling the truth in full, because an <code>alias</code> cannot do more than use the data.</p>"},"p2_19":{"t":"Why Rust Moves by Default","ctx":"Why does assignment invalidate the source?","body":"<p>The fourth obstacle is <span class=\"ea\" data-t=\"p2_8\">invisible coordinate duplication<span class=\"tt\"><span class=\"em-q\">Byte-copying duplicates the coordinate without duplicating the space.</span></span></span>. Byte-copying a <code>String</code> duplicates the heap pointer, creating two owners of one space. Rust prevents this by making assignment transfer ownership rather than copy bytes.</p><p><code>let t = s</code> moves ownership to <code>t</code>, and <code>s</code> becomes invalid. The compiler does not know whether a type&#39;s bytes contain coordinates to managed space. Moving is the safe default.</p><p>Types that contain no coordinates to managed space opt into byte duplication by implementing the <code>Copy</code> trait. <code>i32</code>, <code>bool</code>, <code>f64</code>, <code>char</code>, and tuples of Copy types are all Copy. <code>String</code>, <code>Vec</code>, and any type that manages heap space are not Copy.</p><div class=\"cb\"><code><span class=\"m\">explicit!</span> {\n    <span class=\"k\">let</span> owner(x) = take(<span class=\"n\">5</span>);\n    <span class=\"k\">let</span> owner(y) = mem_copy(x);     <span class=\"c\">// duplicate the bytes</span>\n    <span class=\"m\">println!</span>(<span class=\"s\">\"x = {}, y = {}\"</span>, x, y); <span class=\"c\">// both valid</span>\n}</code></div><p><code>let y = x</code> does not say whether it copies or moves. The compiler decides based on the type. The notation surfaces this decision.</p><div class=\"cb\"><code><span class=\"m\">explicit!</span> {\n    <span class=\"k\">let</span> owner(x1) = take(<span class=\"n\">5</span>);\n    <span class=\"k\">let</span> owner(y1) = take_or_mem_copy(x1);  <span class=\"c\">// i32 is Copy: mem_copy</span>\n    <span class=\"m\">println!</span>(<span class=\"s\">\"x1 = {}, y1 = {}\"</span>, x1, y1);  <span class=\"c\">// both valid</span>\n\n    <span class=\"k\">let</span> owner(x2) = take(<span class=\"m\">String</span>::from(<span class=\"s\">\"hello\"</span>));\n    <span class=\"k\">let</span> owner(y2) = take_or_mem_copy(x2);  <span class=\"c\">// String is not Copy: take</span>\n    <span class=\"m\">println!</span>(<span class=\"s\">\"y2 = {}\"</span>, y2);\n    <span class=\"c\">// x2 is invalid here</span>\n}</code></div><p><code>take_or_mem_copy</code> makes the compiler&#39;s decision visible. Standard Rust writes both operations as <code>let y = x</code>, and the same syntax produces different behavior depending on the type. For programs that need a full independent copy of a non-Copy type, <code>.clone()</code> allocates new space and copies the data.</p>"},"p2_20":{"t":"What mut Means on a Binding","ctx":"Rebinding and writing through a coordinate are different operations.","body":"<p>Every binding so far has been fixed for its scope. A rebindable binding can point to a different value or a different target. The notation writes this as <code>rebindable</code>. Standard Rust writes <code>let mut</code>.</p><div class=\"cb\"><code><span class=\"m\">explicit!</span> {\n    <span class=\"k\">let</span> owner(rebindable(x)) = take(<span class=\"m\">String</span>::from(<span class=\"s\">\"hello\"</span>));\n    x = take(<span class=\"m\">String</span>::from(<span class=\"s\">\"world\"</span>));  <span class=\"c\">// x now holds different space</span>\n    <span class=\"m\">println!</span>(<span class=\"s\">\"{}\"</span>, at(x));            <span class=\"c\">// prints \"world\"</span>\n}</code></div><p>Reassigning <code>x</code> frees the old space, because <code>x</code> was its owner and the owner has moved to different space. Rebinding changes the binding itself, pointing it to a new target. Changing data at a coordinate modifies the contents of the target without changing what the binding points to. These are different operations.</p><p>C++ uses <code>const</code> for both purposes. <code>const int x = 5</code> prevents reassignment. <code>const int& r = x</code> prevents writing at the reference target. Rust separates them. <code>mut</code> on the binding controls reassignment. <code>&mut</code> on the coordinate controls write access. The two are orthogonal.</p><table><thead><tr><th></th><th>Can reassign binding</th><th>Can write at coordinate</th></tr></thead><tbody><tr><td><code>let r = &x</code></td><td>No</td><td>No</td></tr><tr><td><code>let mut r = &x</code></td><td>Yes</td><td>No</td></tr><tr><td><code>let r = &mut x</code></td><td>No</td><td>Yes</td></tr><tr><td><code>let mut r = &mut x</code></td><td>Yes</td><td>Yes</td></tr></tbody></table>"},"p2_21":{"t":"Shared and Exclusive Coordinates","ctx":"Two kinds of coordinates exist. The coherence constraint at the language level.","body":"<p><code>coord_exclusive</code> (<code>&mut T</code>) grants read and write access to one coordinate at a time. <code>coord_shared</code> (<code>&T</code>) grants read access and allows multiple coordinates to coexist. Rust calls <code>&T</code> a shared reference and <code>&mut T</code> a mutable reference.</p><div class=\"cb\"><code><span class=\"m\">explicit!</span> {\n    <span class=\"k\">let</span> owner(s) = take(<span class=\"m\">String</span>::from(<span class=\"s\">\"hello\"</span>));\n    <span class=\"k\">let</span> alias(r1) = coord_shared(s);\n    <span class=\"k\">let</span> alias(r2) = coord_shared(s);\n    <span class=\"m\">println!</span>(<span class=\"s\">\"{}, {}\"</span>, at(r1), at(r2)); <span class=\"c\">// two shared coordinates, safe</span>\n}</code></div><div class=\"cb\"><code><span class=\"m\">explicit!</span> {\n    <span class=\"k\">let</span> owner(<span class=\"k\">mut</span> s) = take(<span class=\"m\">String</span>::from(<span class=\"s\">\"hello\"</span>));\n    <span class=\"k\">let</span> alias(w) = coord_exclusive(s);\n    at(w).push_str(<span class=\"s\">\" world\"</span>); <span class=\"c\">// one exclusive coordinate, safe</span>\n}</code></div><table><thead><tr><th>Rust</th><th>Notation</th></tr></thead><tbody><tr><td><code>let r = &s</code></td><td><code>let alias(r) = coord_shared(s)</code></td></tr><tr><td><code>let w = &mut s</code></td><td><code>let alias(w) = coord_exclusive(s)</code></td></tr><tr><td><code>*r</code></td><td><code>at(r)</code></td></tr><tr><td><code>let mut x</code></td><td><code>owner(rebindable(x))</code></td></tr></tbody></table><p>This is the <span class=\"ea\" data-t=\"p1_8\">coherence constraint<span class=\"tt\"><span class=\"em-q\">The same algebra governs hardware and software.</span></span></span> from the earlier chapter, enforced at the language level. <code>coord_shared</code> corresponds to the Shared state in the protocol. Many processes hold a read-only copy. <code>coord_exclusive</code> corresponds to the Modified/Exclusive states. One process holds the writable copy. The rule is the same, many readers or one writer, and the compiler enforces it at compile time with the same invariant the hardware enforces at runtime.</p><p>Rust&#39;s error messages say &quot;borrowed as immutable&quot; for a shared coordinate and &quot;borrowed as mutable&quot; for an exclusive coordinate. The <span class=\"ea\" data-t=\"p2_26\">borrowing rule<span class=\"tt\"><span class=\"em-q\">Many shared or one exclusive.</span></span></span> derives the rules governing how shared and exclusive coordinates interact. Many shared or one exclusive.</p>"},"p2_22":{"t":"Composing the Vocabulary","ctx":"How do all these pieces fit together?","body":"<p><code>at</code> works on both sides of an assignment. On the right it follows the coordinate and reads the value. On the left it follows the coordinate and writes a new value.</p><div class=\"cb\"><code><span class=\"m\">explicit!</span> {\n    <span class=\"k\">let</span> owner(rebindable(x)) = take(<span class=\"n\">5</span>);\n    <span class=\"k\">let</span> alias(r) = coord_exclusive(x);\n    at(r) = <span class=\"n\">10</span>;                        <span class=\"c\">// write: target now holds 10</span>\n    <span class=\"k\">let</span> owner(y) = mem_copy(at(r));    <span class=\"c\">// read: y gets a copy of 10</span>\n}</code></div><p>An exclusive coordinate suspends the owner for the duration of the coordinate&#39;s liveness. The compiler enforces this as part of the <span class=\"ea\" data-t=\"p2_26\">borrowing rule<span class=\"tt\"><span class=\"em-q\">Many shared or one exclusive.</span></span></span>. The owner resumes when the coordinate expires. <code>take</code> permanently invalidates the source. An exclusive coordinate temporarily suspends it.</p><div class=\"cb\"><code><span class=\"m\">explicit!</span> {\n    <span class=\"k\">let</span> owner(rebindable(x)) = take(<span class=\"n\">5</span>);\n    <span class=\"k\">let</span> alias(r) = coord_exclusive(x);\n    <span class=\"c\">// x is suspended here</span>\n    at(r) = <span class=\"n\">10</span>;\n    <span class=\"c\">// r goes out of scope, x resumes</span>\n    <span class=\"m\">println!</span>(<span class=\"s\">\"{}\"</span>, x);    <span class=\"c\">// prints 10</span>\n}</code></div><p><code>Box<T></code> owns heap space containing a <code>T</code>. Following the coordinate with <code>at</code> reaches the inner value, and <code>take</code> moves that value out, transferring ownership and invalidating the Box.</p><div class=\"cb\"><code><span class=\"m\">explicit!</span> {\n    <span class=\"k\">let</span> owner(b) = take(<span class=\"m\">Box</span>::new(<span class=\"m\">String</span>::from(<span class=\"s\">\"hello\"</span>)));\n    <span class=\"k\">let</span> owner(s) = take(at(b));    <span class=\"c\">// move String out of the Box</span>\n    <span class=\"m\">println!</span>(<span class=\"s\">\"{}\"</span>, s);\n    <span class=\"c\">// b is invalid</span>\n}</code></div><p>Standard Rust writes <code>let s = *b</code>. Dereferencing a <code>Box</code> transfers ownership of the inner value. Regular references do not support this operation.</p><table><thead><tr><th>Notation</th><th>Framework</th></tr></thead><tbody><tr><td><code>owner</code></td><td>SPACE × TIME</td></tr><tr><td><code>alias</code></td><td>COORDINATES</td></tr><tr><td><code>take</code></td><td>Ownership transfer in TIME</td></tr><tr><td><code>coord_exclusive</code></td><td>One COORDINATE, read/write</td></tr><tr><td><code>coord_shared</code></td><td>Many COORDINATES, read only</td></tr><tr><td><code>at</code></td><td>Follow a COORDINATE</td></tr><tr><td><code>mem_copy</code></td><td>Duplicate SPACE, independent owners</td></tr><tr><td><code>take_or_mem_copy</code></td><td>Compiler decides based on type</td></tr><tr><td><code>rebindable</code></td><td>Binding changes target over TIME</td></tr></tbody></table><p>The <code>explicit!</code> notation reveals what standard Rust leaves implicit.</p>"},"p2_23":{"t":"How a Coordinate Can Dangle While Its Owner Lives","ctx":"Ownership prevents dangling after free. But coordinates can dangle for a different reason.","body":"<p>A <code>Vec</code> manages a growable heap buffer. A coordinate to the Vec struct and a coordinate to one of its elements refer to different space. The Vec struct sits on the stack. The element lives in the heap buffer at an address the Vec computes as <code>buffer_ptr + index * sizeof(T)</code>.</p><div class=\"cb\"><code>Stack                         Heap\n┌─────────────┐              ┌───┬───┬───┐\n│      v      │              │ 1 │ 2 │ 3 │\n│  ptr ───────│─────────────&gt;│   │   │   │\n│  len: 3     │              └───┴───┴───┘\n│  cap: 3     │\n└─────────────┘\n\ncoord_shared(v)    → the Vec struct on the stack\ncoord_shared(v[0]) → the first element in the heap buffer</code></div><p><code>v.push(4)</code> can reallocate the heap buffer when capacity runs out. The Vec allocates a larger buffer, copies the elements, and frees the old one. A coordinate created before the push still holds the old address. The owner <code>v</code> remains alive. A coordinate to the Vec struct would still work. The element coordinate points to reclaimed space because the method changed the internal state that determines the address.</p><div class=\"cb\"><code>    ┌───┐\n    │ A │  let r = &amp;v[2]\n    └─┬─┘\n      ▼\n    ┌───┐\n    │ B │  v.push(4) — may reallocate, old buffer freed\n    └─┬─┘\n      ▼\n    ┌───┐\n    │ C │  println!(\"{}\", r) — r points to freed memory ✗\n    └───┘</code></div><p>The path from B to C exists. A method changed internal state while a coordinate computed from that state was still alive. This is the same pattern behind the <span class=\"ea\" data-t=\"p1_2\">push-while-borrowing error<span class=\"tt\"><span class=\"em-q\">The compiler rejects code that looks right.</span></span></span> from chapter 1, but now the mechanism is visible. The owner lives. The derived coordinate dangles.</p><p>A change to <code>buffer_ptr</code> makes the element address stale. The element coordinate depends on state the Vec controls, and push can change that state.</p>"},"p2_24":{"t":"Why Derived Data Needs a Sync Strategy","ctx":"The element coordinate dangled because it was computed from internal state. This is a general pattern.","body":"<p>The element coordinate is derived data. The address <code>buffer_ptr + index * sizeof(T)</code> is computed from the Vec&#39;s internal state. The derived value becomes stale if that state changes. A formula <code>a = f(b)</code> creates a dependency. <code>a</code> becomes stale if <code>b</code> changes and no recomputation follows.</p><p>CPU caches, database indexes, element coordinates, and struct field dependencies are all instances of the same <span class=\"ea\" data-t=\"p1_12\">pattern<span class=\"tt\"><span class=\"em-q\">Every copy is derived data.</span></span></span>. Two kinds of derived data appear in programs. Derived coordinates are addresses computed from internal state. Derived values are fields whose values depend on another field. Both create invalidation problems.</p><p>A compiler managing coordinates needs a sync strategy. Either prevent the source from changing while derived data exists, or recompute derived data after every change. Recomputation requires knowing the dependency formula, which the compiler does not have for arbitrary user types. Prevention is the feasible option.</p>"},"p2_25":{"t":"Why the Compiler Cannot Distinguish Safe Methods","ctx":"The compiler needs to block dangerous operations. Can it tell which methods are dangerous?","body":"<p><code>sort</code> takes exclusive access to a Vec and rearranges elements in place. <code>push</code> takes exclusive access and can reallocate the buffer. Whether <code>push</code> reallocates depends on runtime values. The current length, the current capacity, and the allocator&#39;s behavior all determine the outcome. This is <span class=\"ea\" data-t=\"p2_10\">undecidable<span class=\"tt\"><span class=\"em-q\">No algorithm can determine this for all programs.</span></span></span>. No static analysis can determine for all programs whether a particular call to <code>push</code> will reallocate.</p><p>The compiler cannot tell which exclusive-access methods restructure internal space and which merely rearrange it. The distinction depends on runtime state. The compiler blocks any method that takes exclusive access while an element coordinate exists. This is conservative. <code>sort</code> would be safe, but conservatism is the price of soundness.</p>"},"p2_26":{"t":"The Borrowing Rule","ctx":"The compiler blocks exclusive access while a coordinate exists. How long does a coordinate exist?","body":"<p>Three levels of structure shape the answer. Source text organizes names into a scope hierarchy. A name enters scope at its declaration and leaves when the enclosing block ends. The language&#39;s semantics operate above that hierarchy. Function arguments carry data into nested scopes, return values carry it out, and assignments move data between bindings. The RAM machine sits below both levels and has no scope structure at all.</p><p>Coordinate liveness is a data-flow property. A coordinate&#39;s last use can fall well before its name goes out of scope. The compiler tracks each coordinate from creation to last use on the control flow graph, the same tool used <span class=\"ea\" data-t=\"p2_4\">earlier<span class=\"tt\"><span class=\"em-q\">The compiler checks all paths and rejects when any is unsafe.</span></span></span> to detect access to reclaimed space. Rust calls this analysis <strong>non-lexical lifetimes</strong> (NLL). Before Rust 2018, the compiler used scope boundaries, which rejected valid programs unnecessarily.</p><div class=\"cb\"><code><span class=\"k\">let</span> r = &amp;v[<span class=\"n\">0</span>];\n<span class=\"m\">println!</span>(<span class=\"s\">\"{}\"</span>, r);   <span class=\"c\">// last use of r</span>\nv.push(<span class=\"n\">4</span>);           <span class=\"c\">// allowed: r is no longer alive</span></code></div><div class=\"cb\"><code><span class=\"k\">let</span> r = &amp;v[<span class=\"n\">0</span>];\nv.push(<span class=\"n\">4</span>);           <span class=\"c\">// REJECTED: r is still alive</span>\n<span class=\"m\">println!</span>(<span class=\"s\">\"{}\"</span>, r);</code></div><p>The only difference is the order of the last two statements. In the first program, <code>r</code>&#39;s last use is the <code>println!</code>, and <code>push</code> comes after. In the second, <code>push</code> falls inside <code>r</code>&#39;s liveness range. The compiler rejects the second and accepts the first.</p><p>This generalizes to the full rule. A space can have many <span class=\"ea\" data-t=\"p2_21\">shared coordinates<span class=\"tt\"><span class=\"em-q\">Read access, multiple coordinates coexist.</span></span></span> or one exclusive coordinate at any point in the program. Code holding a shared coordinate can read. The borrowing rule blocks methods requiring exclusive access, so shared coordinates coexist safely. Code holding an exclusive coordinate can call any method, so no other coordinate to the same space can be alive while it exists. This is the borrowing rule, and it governs every reference in every Rust program.</p>"},"p2_27":{"t":"Why the Borrowing Rule Applies to Every Type","ctx":"Vec manages heap space. But the rule applies to i32 too. Why?","body":"<p>An <code>i32</code> has no managed internal space. Two coordinates to the same <code>i32</code> cannot produce a dangling pointer because there is no internal space to restructure. The compiler could apply the borrowing rule only to types that manage internal allocations and leave simple types unconstrained.</p><p>Determining which types manage internal space requires analyzing field dependencies. A struct might hold a raw pointer to a heap allocation, or contain a field whose value depends on another field. Deciding whether a type has such dependencies is <span class=\"ea\" data-t=\"p2_10\">undecidable<span class=\"tt\"><span class=\"em-q\">No algorithm answers correctly for all inputs.</span></span></span> for the same reasons described earlier. The compiler applies the borrowing rule uniformly to every type.</p><div class=\"cb\"><code>The compiler needs a sync strategy for derived data\n│\n├── Derived from managed space\n│     push changes buffer_ptr → element coordinate dangles\n│\n├── Derived from struct fields\n│     field a = f(field b) → changing b makes a stale\n│\n└── The compiler cannot distinguish these cases\n      → uniform rule: many shared or one exclusive</code></div><p>The uniform rule is a sync strategy for <span class=\"ea\" data-t=\"p2_24\">derived data<span class=\"tt\"><span class=\"em-q\">Any derived value becomes stale when its source changes.</span></span></span> in general. An element coordinate is derived from the Vec&#39;s buffer pointer. A struct field value can be derived from another field. A cache entry is derived from the data it caches. The compiler cannot know which types have these dependencies, so it prevents any source from changing while a coordinate derived from it exists.</p>"},"p2_28":{"t":"The Chapter 1 Programs and the Borrowing Rule","ctx":"The borrowing rule derived. Now see it applied to every rejected program from chapter 1.","body":"<p><strong>Push while borrowing.</strong> <code>r</code> is a shared coordinate to the first element. <code>push</code> requires exclusive access to <code>v</code>. A shared coordinate and an exclusive coordinate to overlapping space exist at the same time.</p><div class=\"cb\"><code><span class=\"k\">let</span> <span class=\"k\">mut</span> v = <span class=\"m\">vec!</span>[<span class=\"n\">1</span>, <span class=\"n\">2</span>, <span class=\"n\">3</span>];\n<span class=\"k\">let</span> r = &amp;v[<span class=\"n\">0</span>];\nv.push(<span class=\"n\">4</span>);\n<span class=\"m\">println!</span>(<span class=\"s\">\"{}\"</span>, r);</code></div><div class=\"er\"><strong>error[E0502]:</strong> cannot borrow <code>v</code> as mutable because it is also borrowed as immutable</div><p><strong>Iterator invalidation.</strong> The <code>for</code> loop creates an iterator that holds a shared coordinate to <code>v</code>. <code>push</code> requires exclusive access.</p><div class=\"cb\"><code><span class=\"k\">let</span> <span class=\"k\">mut</span> v = <span class=\"m\">vec!</span>[<span class=\"n\">1</span>, <span class=\"n\">2</span>, <span class=\"n\">3</span>];\n<span class=\"k\">for</span> x <span class=\"k\">in</span> &amp;v {\n    v.push(*x);\n}</code></div><div class=\"er\"><strong>error[E0502]:</strong> cannot borrow <code>v</code> as mutable because it is also borrowed as immutable</div><p><strong>Closure capturing.</strong> The closure captures <code>v</code> by exclusive coordinate because it calls <code>push</code>. <code>println!</code> requires a shared coordinate. The closure exists from creation to last use, and the <code>println!</code> falls within that range.</p><div class=\"cb\"><code><span class=\"k\">let</span> <span class=\"k\">mut</span> v = <span class=\"m\">vec!</span>[<span class=\"n\">1</span>, <span class=\"n\">2</span>, <span class=\"n\">3</span>];\n<span class=\"k\">let</span> closure = || v.push(<span class=\"n\">4</span>);\n<span class=\"m\">println!</span>(<span class=\"s\">\"{:?}\"</span>, v);\nclosure();</code></div><div class=\"er\"><strong>error[E0502]:</strong> cannot borrow <code>v</code> as immutable because it is also borrowed as mutable</div><p><strong>Two exclusive coordinates.</strong> Two <code>&mut</code> to the same <code>String</code>. Either one could call methods that restructure the internal buffer while the other depends on the old state.</p><div class=\"cb\"><code><span class=\"k\">let</span> <span class=\"k\">mut</span> s = <span class=\"m\">String</span>::from(<span class=\"s\">\"hello\"</span>);\n<span class=\"k\">let</span> r1 = &amp;<span class=\"k\">mut</span> s;\n<span class=\"k\">let</span> r2 = &amp;<span class=\"k\">mut</span> s;\n<span class=\"m\">println!</span>(<span class=\"s\">\"{}, {}\"</span>, r1, r2);</code></div><div class=\"er\"><strong>error[E0499]:</strong> cannot borrow <code>s</code> as mutable more than once at a time</div><p>Each program maps to the same pattern. A coordinate exists while the program attempts an operation that could invalidate it.</p><p>C++ has the same vocabulary of shared and exclusive access. <code>const T&</code> grants read-only access. <code>T&</code> grants read and write. C++ does not enforce the borrowing rule at compile time. The push-while-borrowing and iterator invalidation examples compile in C++ without error and produce undefined behavior at runtime.</p><div class=\"cb\"><code><span class=\"c\">// C++ — compiles, undefined behavior</span>\n<span class=\"k\">std</span>::vector&lt;<span class=\"k\">int</span>&gt; v = {<span <span class=\"k\">class</span>=\"n\">1</span>, <span <span class=\"k\">class</span>=\"n\">2</span>, <span <span class=\"k\">class</span>=\"n\">3</span>};\n<span class=\"k\">int</span>&amp; r = v[<span <span class=\"k\">class</span>=\"n\">0</span>];\nv.push_back(<span <span class=\"k\">class</span>=\"n\">4</span>);   <span class=\"c\">// may reallocate</span>\n<span class=\"k\">std</span>::<span class=\"k\">cout</span> &lt;&lt; r;    <span class=\"c\">// r may point to freed memory</span></code></div><p>The C++ standard calls this the programmer&#39;s responsibility. The compiler accepts the code. Whether the program crashes, prints garbage, or appears to work depends on the allocator&#39;s behavior at runtime.</p><p>Parallel execution introduces a separate, independent reason for the same rule. A <span class=\"ea\" data-t=\"p1_18\">data race<span class=\"tt\"><span class=\"em-q\">Two threads, one address, no synchronization.</span></span></span> requires parallel TIME, shared SPACE, and multiple COORDINATES where at least one writes. One thread reading an <code>i32</code> while another writes to it can produce a torn read, depending on the hardware. The <code>i32</code> has no managed internal space. Parallel access to the same space creates the danger regardless of whether any type manages internal allocations.</p><p>The borrowing rule prevents both dangers with the same mechanism. Shared coordinates allow parallel reads because no writer can change the state they depend on. An exclusive coordinate prevents parallel access by ensuring no other coordinate exists. The rule protects managed space from restructuring in sequential code and prevents data races when execution is parallel.</p><p>The <span class=\"ea\" data-t=\"p1_3\">thread safety errors<span class=\"tt\"><span class=\"em-q\">The same borrowing rule prevents data races.</span></span></span> from chapter 1 follow from the same rule.</p>"},"p3_1":{"t":"What the Compiler Can Distinguish","ctx":"The borrowing rule is an approximation. Where does it lose precision?","body":"<p>The analysis tracks coordinates. The granularity of that tracking determines which simultaneous accesses the compiler can prove safe.</p><p><strong>Struct fields.</strong> <code>p.x</code> and <code>p.y</code> are distinct. The type declaration places them at distinct offsets, and field names are literal in source text. The compiler can prove that coordinates to <code>p.x</code> and coordinates to <code>p.y</code> point to disjoint space.</p><p><strong>Array elements with literal indices.</strong> <code>v[0]</code> and <code>v[1]</code> refer to different elements. The literal indices are visible in source text, so the compiler could distinguish them. Rust treats all index expressions as potentially overlapping. This is a design choice.</p><p><strong>Array elements with computed indices.</strong> <code>v[i]</code> and <code>v[j]</code> where <code>i</code> and <code>j</code> come from computation. Whether <code>i == j</code> depends on runtime values. Proving it requires solving the <span class=\"ea\" data-t=\"p2_10\">halting problem<span class=\"tt\"><span class=\"em-q\">Turing proved it in 1936.</span></span></span>.</p><table><thead><tr><th>Coordinates to</th><th>Distinguishable?</th><th>Why</th></tr></thead><tbody><tr><td><code>p.x</code> vs <code>p.y</code></td><td>Yes</td><td>Field names literal in source</td></tr><tr><td><code>v[0]</code> vs <code>v[1]</code></td><td>Could be</td><td>Rust&#39;s design choice</td></tr><tr><td><code>v[i]</code> vs <code>v[j]</code></td><td>No</td><td>May require solving halting problem</td></tr></tbody></table><p>Branches and pointer aliasing share this structure. Whether <code>*p</code> and <code>*q</code> point to the same space depends on runtime values. The compiler assumes worst case for both. The compiler handles what source text names. Computed values belong to data flow, and data flow is where undecidability enters.</p>"},"p3_2":{"t":"Why Compilers Skip Decidable Cases","ctx":"Literal indices are decidable. Why does the compiler still reject them?","body":"<p>The literal index <code>v[0]</code> is decidable. So is a function call <code>pick(&a, &b, true)</code> where the argument is a constant. A human sees which branch executes. The compiler could too, using constant propagation and inlining.</p><p>General-purpose compilers favor predictability. Code that compiles when the argument is <code>true</code> fails when <code>true</code> becomes <code>some_function()</code>, and programmers cannot predict which expressions cross the decidability boundary. Identifying which expressions fall into decidable subsets is itself expensive, and coverage is narrow.</p><p>Specialized compilers do analyze decidable cases. Fortran compilers use polyhedral analysis for loop parallelization. ML frameworks require static tensor shapes. The techniques exist and work for specific domains. Rust chose general-purpose consistency over domain-specific precision. The compiler treats all index expressions and all branch conditions the same way. A program that compiles with a constant will still compile when that constant becomes a variable.</p>"},"p3_3":{"t":"Signatures Lose Field-Level Information","ctx":"The compiler distinguishes struct fields inside a body. What happens across a function call?","body":"<p>Inside a function body, the compiler sees field-level coordinate access. <code>&d.field1</code> and <code>&d.field2</code> point to disjoint space, and the compiler knows it.</p><div class=\"cb\"><code><span class=\"k\">let</span> r1 = &amp;d.field1;    <span class=\"c\">// coordinates to d.field1</span>\n<span class=\"k\">let</span> r2 = &amp;d.field2;    <span class=\"c\">// coordinates to d.field2</span>\n<span class=\"c\">// disjoint: yes</span></code></div><p>Across a function call, only the signature is visible. A method that borrows <code>self</code> borrows the whole struct.</p><div class=\"cb\"><code><span class=\"k\">let</span> r1 = d.get_field1();   <span class=\"c\">// signature says: borrows d</span>\n<span class=\"k\">let</span> r2 = d.get_field2();   <span class=\"c\">// signature says: borrows d</span>\n<span class=\"c\">// disjoint: the compiler cannot tell</span></code></div><p>Signatures encode coordinate connections at struct granularity. Field-level information does not cross function boundaries. Types describe whole structs and encode connections at that level. The compiler sees two borrows of <code>d</code> where a human sees two borrows of disjoint fields.</p>"},"p3_4":{"t":"The Mirror","ctx":"The borrowing rule is derived. The compiler tightened three dimensions to make analysis possible.","body":"<table><thead><tr><th>Dimension</th><th>What was tightened</th><th>The patch</th></tr></thead><tbody><tr><td>SPACE</td><td>Each allocation has one owner</td><td><span class=\"ea\" data-t=\"p2_14\">Ownership<span class=\"tt\"><span class=\"em-q\">One owner, scope-tied lifetime.</span></span></span>, move by default</td></tr><tr><td>TIME</td><td>Space is reclaimed when the owner&#39;s scope ends</td><td>Scope-tied lifetimes</td></tr><tr><td>COORDINATES</td><td>One exclusive or many shared, never both</td><td><span class=\"ea\" data-t=\"p2_13\">Constrained references<span class=\"tt\"><span class=\"em-q\">Every reference has a known target.</span></span></span>, <span class=\"ea\" data-t=\"p2_26\">borrowing rule<span class=\"tt\"><span class=\"em-q\">Many shared or one exclusive.</span></span></span></td></tr></tbody></table><p>The tightening is the price of compile-time proof. The proof is <span class=\"ea\" data-t=\"p2_11\">sound<span class=\"tt\"><span class=\"em-q\">Never accept unsafe code.</span></span></span>. The compiler accepts no unsafe program. But the proof is approximate, and the compiler rejects some safe programs. The rejected programs are safe because the programmer knows something the compiler cannot verify.</p><p>Every rejection is an over-constraint in one of the three dimensions. Every fix relaxes that dimension. The question is always the same. Which dimension is too tight, and how far do you need to loosen it?</p><table><thead><tr><th></th><th>Type-level encoding</th><th>Runtime checking</th><th>Manual assertion</th></tr></thead><tbody><tr><td><strong>SPACE</strong></td><td><code>Cow</code>, entry API, arena indices</td><td><code>Rc</code>, <code>Arc</code></td><td>raw pointer, <code>unsafe</code></td></tr><tr><td><strong>TIME</strong></td><td>Lifetime annotations, <code>'static</code></td><td><code>RefCell</code> borrow tracking</td><td><code>unsafe</code> lifetime cast</td></tr><tr><td><strong>COORDINATES</strong></td><td><code>split_at_mut</code>, <code>NonNull</code></td><td><code>Mutex</code>, <code>RwLock</code></td><td>raw pointer deref</td></tr></tbody></table><p>Each column to the right costs more. Type-level encoding costs design effort but nothing at runtime. Runtime checking costs performance. Manual assertion costs safety. The programmer asserts the invariant the compiler cannot prove, and no one checks.</p>"},"p3_5":{"t":"Relaxing Space","ctx":"What was tightened: each allocation has one owner. Where it over-rejects and how to fix it.","body":"<p><strong>What was tightened.</strong> Each allocation has one owner. When the owner goes out of scope, the space is reclaimed. Assignment moves <span class=\"ea\" data-t=\"p2_14\">ownership<span class=\"tt\"><span class=\"em-q\">One owner, scope-tied lifetime.</span></span></span>, and the source becomes invalid.</p><p><strong>Where it over-rejects.</strong> Multiple parts of the program need access to the same data, and no single binding is the natural owner. A configuration struct shared by several subsystems. A graph where multiple edges reach the same node. A cache that outlives any single caller.</p><p><strong>Type-level encoding: clone, copy, and arena indices.</strong></p><p><code>Clone</code> duplicates the data. Each copy is independent. Ownership is trivial because each binding owns its own space. The cost is CPU time and memory.</p><div class=\"cb\"><code><span class=\"k\">let</span> config = load_config();\n<span class=\"k\">let</span> for_service_a = config.clone();\n<span class=\"k\">let</span> for_service_b = config.clone();</code></div><p><code>Cow</code> (clone on write) defers the clone until mutation is needed.</p><div class=\"cb\"><code><span class=\"k\">use</span> std::borrow::Cow;\n<span class=\"k\">fn</span> <span class=\"m\">process</span>(input: Cow&lt;<span class=\"m\">str</span>&gt;) -&gt; Cow&lt;<span class=\"m\">str</span>&gt; {\n    <span class=\"k\">if</span> input.contains(<span class=\"s\">\"bad\"</span>) {\n        Cow::Owned(input.replace(<span class=\"s\">\"bad\"</span>, <span class=\"s\">\"good\"</span>))\n    } <span class=\"k\">else</span> {\n        input <span class=\"c\">// no clone</span>\n    }\n}</code></div><p><code>Copy</code> opts small types into automatic duplication. <code>i32</code>, <code>bool</code>, and <code>f64</code> have no managed space. Duplicating their bytes produces two independent values.</p><p>Arena indices eliminate coordinates entirely. A single collection owns all nodes. Edges are pairs of <code>usize</code> integers, freely copyable, with no lifetime tracking. The borrow checker does not track indices.</p><div class=\"cb\"><code><span class=\"k\">struct</span> Graph {\n    nodes: <span class=\"m\">Vec</span>&lt;NodeData&gt;,\n    edges: <span class=\"m\">Vec</span>&lt;(<span class=\"m\">usize</span>, <span class=\"m\">usize</span>)&gt;,\n}</code></div><p>Index validity becomes the programmer&#39;s responsibility, or a runtime check with generational indices.</p><div class=\"cb\"><code><span class=\"k\">struct</span> NodeId {\n    index: <span class=\"m\">usize</span>,\n    generation: <span class=\"m\">u64</span>,\n}</code></div><p>This is the only pattern that resolves all <span class=\"ea\" data-t=\"p1_13\">three dimensions<span class=\"tt\"><span class=\"em-q\">Where data lives. When things happen. How you reach it.</span></span></span> simultaneously. The single collection centralizes SPACE. All nodes live as long as the collection, making TIME uniform. Indices are <code>Copy</code> integers, so COORDINATES disappear entirely. The Rust ecosystem converges on this pattern for anything graph-shaped. <code>petgraph</code>, <code>slotmap</code>, and most game engines all use arena indices.</p><p><strong>Runtime checking: Rc and Arc.</strong></p><p><code>Rc</code> and <code>Arc</code> let multiple handles share ownership. The value lives until the last handle drops. A runtime counter replaces the single-owner constraint.</p><div class=\"cb\"><code><span class=\"k\">use</span> std::rc::<span class=\"m\">Rc</span>;\n<span class=\"k\">let</span> config = <span class=\"m\">Rc</span>::new(AppConfig::load());\n<span class=\"k\">let</span> service_a = ServiceA::new(<span class=\"m\">Rc</span>::clone(&amp;config));\n<span class=\"k\">let</span> service_b = ServiceB::new(<span class=\"m\">Rc</span>::clone(&amp;config));\n<span class=\"c\">// config lives until all three handles drop</span></code></div><p>Cycles leak memory because each node in the cycle keeps the next alive. <code>Weak</code> breaks cycles by providing a non-owning handle.</p><p><code>Rc</code> is for single-threaded code. <code>Arc</code> is the atomic version for sharing across threads. The cost is the reference count update on every clone and drop.</p><p><strong>Manual assertion: raw pointers.</strong></p><p><code>*const T</code> and <code>*mut T</code> have no ownership semantics. Creating them is safe, but dereferencing them requires <code>unsafe</code>. The programmer asserts that the pointed-to space exists and is valid. No one checks.</p>"},"p3_6":{"t":"Relaxing Time","ctx":"What was tightened: scope-tied lifetimes. Where it over-rejects and how to fix it.","body":"<p><strong>What was tightened.</strong> Space is reclaimed when the owner&#39;s scope ends. References must not outlive their target. Lifetimes are checked at compile time.</p><p><strong>Where it over-rejects.</strong> Coordinate lifetimes overlap when they should not, but the compiler cannot see the ordering. A reference created before a mutation is used after it, but on a different branch. A function returns a reference to one of its inputs, and the caller knows which one, but the signature declares both.</p><p><strong>Type-level encoding: reordering, scoping, and lifetime annotations.</strong></p><p>Reorder operations so the first coordinate&#39;s lifetime ends before the second begins.</p><div class=\"cb\"><code><span class=\"k\">let</span> <span class=\"k\">mut</span> v = <span class=\"m\">vec!</span>[<span class=\"n\">1</span>, <span class=\"n\">2</span>, <span class=\"n\">3</span>];\n<span class=\"k\">let</span> first = v[<span class=\"n\">0</span>]; <span class=\"c\">// read the value — coordinate ends</span>\nv.push(<span class=\"n\">4</span>);        <span class=\"c\">// exclusive access, no conflict</span>\n<span class=\"m\">println!</span>(<span class=\"s\">\"{}\"</span>, first); <span class=\"c\">// first is a copy, not a coordinate</span></code></div><p>Scoping with <code>{}</code> forces a coordinate to die before the next operation.</p><div class=\"cb\"><code><span class=\"k\">let</span> <span class=\"k\">mut</span> v = <span class=\"m\">vec!</span>[<span class=\"n\">1</span>, <span class=\"n\">2</span>, <span class=\"n\">3</span>];\n{\n    <span class=\"k\">let</span> r = &amp;v[<span class=\"n\">0</span>];\n    <span class=\"m\">println!</span>(<span class=\"s\">\"{}\"</span>, r);\n} <span class=\"c\">// r dies here</span>\nv.push(<span class=\"n\">4</span>); <span class=\"c\">// safe</span></code></div><p><span class=\"ea\" data-t=\"p2_16\">Lifetime annotations<span class=\"tt\"><span class=\"em-q\">Declaring data flow across function boundaries.</span></span></span> declare cross-scope dependencies the compiler cannot infer.</p><div class=\"cb\"><code><span class=\"k\">fn</span> <span class=\"m\">first</span>&lt;'a&gt;(list: &amp;'a [<span class=\"m\">i32</span>]) -&gt; &amp;'a <span class=\"m\">i32</span> {\n    &amp;list[<span class=\"n\">0</span>]\n}</code></div><p>Adding <code>T: 'a</code> constrains a generic. Adding <code>'a: 'b</code> declares containment. Same mechanism at different sites.</p><p>Separate read and write passes eliminate aliasing conflicts by separating the operations in time.</p><div class=\"cb\"><code><span class=\"c\">// Read pass: shared coordinates</span>\n<span class=\"k\">let</span> indices: <span class=\"m\">Vec</span>&lt;<span class=\"m\">usize</span>&gt; = data.iter()\n    .enumerate()\n    .filter(|(_, v)| v.should_update())\n    .map(|(i, _)| i)\n    .collect();\n\n<span class=\"c\">// Write pass: exclusive coordinate</span>\n<span class=\"k\">for</span> i <span class=\"k\">in</span> indices {\n    data[i].update();\n}</code></div><p>Building new collections avoids the conflict entirely. The iterator holds a shared coordinate to the input for the entire chain. Nothing mutates the input.</p><div class=\"cb\"><code><span class=\"k\">let</span> result: <span class=\"m\">Vec</span>&lt;_&gt; = input.iter()\n    .filter(|x| x.is_valid())\n    .map(|x| x.transform())\n    .collect();</code></div><p><strong>Index-based graph traversal.</strong> Graph algorithms use indices to traverse while holding a shared coordinate to the graph. The algorithm&#39;s own state (visited set, queue, result list) lives in separate owned space.</p><div class=\"cb\"><code><span class=\"k\">fn</span> <span class=\"m\">bfs</span>(graph: &amp;Graph, start: NodeId) -&gt; <span class=\"m\">Vec</span>&lt;NodeId&gt; {\n    <span class=\"k\">let</span> <span class=\"k\">mut</span> visited = HashSet::new();\n    <span class=\"k\">let</span> <span class=\"k\">mut</span> queue = VecDeque::new();\n    queue.push_back(start);\n    visited.insert(start);\n    <span class=\"k\">while</span> <span class=\"k\">let</span> <span class=\"m\">Some</span>(node) = queue.pop_front() {\n        <span class=\"k\">for</span> &amp;neighbor <span class=\"k\">in</span> &amp;graph.edges[node.index] {\n            <span class=\"k\">if</span> visited.insert(neighbor) {\n                queue.push_back(neighbor);\n            }\n        }\n    }\n    visited.into_iter().collect()\n}</code></div><p>The graph holds a shared coordinate for the entire traversal. The visited set and queue are separate owned space. No aliasing conflict arises because the algorithm reads the graph and writes only to its own state.</p><p><strong>Runtime checking: RefCell.</strong></p><p><code>RefCell</code> enforces the <span class=\"ea\" data-t=\"p2_26\">borrowing rule<span class=\"tt\"><span class=\"em-q\">Many shared or one exclusive.</span></span></span> at runtime instead of compile time. <code>.borrow()</code> returns a shared guard. <code>.borrow_mut()</code> returns an exclusive guard. Multiple shared guards can coexist. An exclusive guard panics if any other guard exists.</p><div class=\"cb\"><code><span class=\"k\">use</span> std::cell::<span class=\"m\">RefCell</span>;\n<span class=\"k\">let</span> data = <span class=\"m\">RefCell</span>::new(<span class=\"m\">vec!</span>[<span class=\"n\">1</span>, <span class=\"n\">2</span>, <span class=\"n\">3</span>]);\n<span class=\"k\">let</span> r1 = data.borrow();     <span class=\"c\">// shared: counter increments</span>\n<span class=\"k\">let</span> r2 = data.borrow();     <span class=\"c\">// shared: counter increments</span>\ndrop(r1);                   <span class=\"c\">// counter decrements</span>\ndrop(r2);                   <span class=\"c\">// counter decrements</span>\n<span class=\"k\">let</span> <span class=\"k\">mut</span> w = data.borrow_mut(); <span class=\"c\">// exclusive: panics if counter &gt; 0</span>\nw.push(<span class=\"n\">4</span>);</code></div><p>The same rule the compiler checks at compile time, enforced at runtime with a borrow counter.</p><p><strong>Message passing: channels.</strong></p><p>Channels serialize access across time by moving data from sender to receiver.</p><div class=\"cb\"><code><span class=\"k\">use</span> std::sync::mpsc;\n<span class=\"k\">let</span> (tx, rx) = mpsc::channel();\nstd::thread::spawn(<span class=\"k\">move</span> || {\n    <span class=\"k\">let</span> data = <span class=\"m\">vec!</span>[<span class=\"n\">1</span>, <span class=\"n\">2</span>, <span class=\"n\">3</span>];\n    tx.send(data).unwrap(); <span class=\"c\">// data moves to channel</span>\n    <span class=\"c\">// data is invalid here</span>\n});\n<span class=\"k\">let</span> received = rx.recv().unwrap(); <span class=\"c\">// received owns the data</span></code></div><p>Sending data moves <span class=\"ea\" data-t=\"p2_14\">ownership<span class=\"tt\"><span class=\"em-q\">One owner, scope-tied lifetime.</span></span></span>. The sender&#39;s binding becomes invalid. The receiver&#39;s binding becomes the owner. At any moment, one thread owns the data. This is the same primitive the <span class=\"ea\" data-t=\"p1_10\">coherence protocol<span class=\"tt\"><span class=\"em-q\">The old holder surrenders the token, the new holder receives it.</span></span></span> uses. The old holder surrenders the token, the new holder receives it.</p><p><strong>Drain and rebuild.</strong> <code>drain()</code> transfers ownership of each element from the collection to the loop body. The loop can move, modify, or drop each element without conflicting with the collection.</p><p><strong>Swap to extract.</strong> <code>mem::swap</code>, <code>mem::replace</code>, and <code>mem::take</code> extract a value at an exclusive coordinate while leaving valid space behind. The owner always owns something after the operation.</p><div class=\"cb\"><code><span class=\"k\">fn</span> <span class=\"m\">rotate_name</span>(person: &amp;<span class=\"k\">mut</span> Person) -&gt; <span class=\"m\">String</span> {\n    <span class=\"k\">let</span> old = std::mem::take(&amp;<span class=\"k\">mut</span> person.name); <span class=\"c\">// leaves String::new()</span>\n    person.name = <span class=\"m\">format!</span>(<span class=\"s\">\"former_{}\"</span>, old);\n    old\n}</code></div><p><strong>Manual assertion: unsafe lifetime casts.</strong></p><p><code>transmute</code> and raw pointers can extend or erase lifetimes. The programmer asserts that the reference outlives its use. The compiler cannot verify this.</p>"},"p3_7":{"t":"Relaxing Coordinates","ctx":"What was tightened: one exclusive or many shared. Where it over-rejects and how to fix it.","body":"<p><strong>What was tightened.</strong> One exclusive coordinate or many shared coordinates at any point. No other combination exists.</p><p><strong>Where it over-rejects.</strong> Two parts of the code need exclusive access to different parts of the same space. The compiler sees one coordinate to the whole and rejects the second. Or a struct needs to mutate an internal field during a shared access. The method takes <code>&self</code> but needs to write.</p><p><strong>Type-level encoding: split and partition.</strong></p><p><code>split_at_mut</code> produces two exclusive coordinates to non-overlapping regions. The return type encodes the disjointness.</p><div class=\"cb\"><code><span class=\"k\">let</span> <span class=\"k\">mut</span> v = <span class=\"m\">vec!</span>[<span class=\"n\">1</span>, <span class=\"n\">2</span>, <span class=\"n\">3</span>, <span class=\"n\">4</span>, <span class=\"n\">5</span>, <span class=\"n\">6</span>];\n<span class=\"k\">let</span> (left, right) = v.split_at_mut(<span class=\"n\">3</span>);\nleft[<span class=\"n\">0</span>] = <span class=\"n\">10</span>;\nright[<span class=\"n\">0</span>] = <span class=\"n\">40</span>; <span class=\"c\">// different region, no conflict</span></code></div><p>Struct fields split automatically. The compiler knows <code>person.name</code> and <code>person.age</code> occupy disjoint space.</p><div class=\"cb\"><code><span class=\"k\">let</span> name = &amp;<span class=\"k\">mut</span> person.name;\n<span class=\"k\">let</span> age = &amp;<span class=\"k\">mut</span> person.age; <span class=\"c\">// different field, compiler allows</span></code></div><p>Binding struct fields individually before a closure achieves the same at the field level.</p><p>Borrowing instead of consuming means passing <code>&</code> where ownership is not needed. The caller keeps ownership.</p><div class=\"cb\"><code><span class=\"k\">fn</span> <span class=\"m\">print_len</span>(s: &amp;<span class=\"m\">String</span>) {\n    <span class=\"m\">println!</span>(<span class=\"s\">\"{}\"</span>, s.len());\n}\n<span class=\"k\">let</span> s = <span class=\"m\">String</span>::from(<span class=\"s\">\"hello\"</span>);\nprint_len(&amp;s);\n<span class=\"m\">println!</span>(<span class=\"s\">\"{}\"</span>, s); <span class=\"c\">// s still alive</span></code></div><p><strong>Runtime checking: interior mutability.</strong></p><p>The <span class=\"ea\" data-t=\"p2_26\">aliasing rule<span class=\"tt\"><span class=\"em-q\">Many shared or one exclusive.</span></span></span> guarantees that no code writes during shared access. Sometimes a struct needs to write during shared access for a cache lookup, a lazy initialization, or a counter increment.</p><p><code>Cell<T></code> never exposes a coordinate to its contents. <code>.get()</code> copies the value out, <code>.set()</code> overwrites it. Works for <code>Copy</code> types.</p><div class=\"cb\"><code><span class=\"k\">use</span> std::cell::<span class=\"m\">Cell</span>;\n<span class=\"k\">let</span> counter = <span class=\"m\">Cell</span>::new(<span class=\"n\">0</span>);\n<span class=\"k\">let</span> a = &amp;counter;\n<span class=\"k\">let</span> b = &amp;counter;\na.set(a.get() + <span class=\"n\">1</span>);\nb.set(b.get() + <span class=\"n\">1</span>);\n<span class=\"m\">assert_eq!</span>(counter.get(), <span class=\"n\">2</span>);</code></div><p><code>Mutex</code> and <code>RwLock</code> enforce the same rule across threads. <code>Mutex</code> provides exclusive access, and callers block until the lock is free. <code>RwLock</code> allows many readers or one writer, the same <span class=\"ea\" data-t=\"p1_8\">constraint<span class=\"tt\"><span class=\"em-q\">Many readers or one exclusive writer.</span></span></span> enforced at runtime with blocking.</p><div class=\"cb\"><code><span class=\"k\">use</span> std::sync::<span class=\"m\">Mutex</span>;\n<span class=\"k\">let</span> data = <span class=\"m\">Mutex</span>::new(<span class=\"m\">Vec</span>::new());\n<span class=\"k\">let</span> guard = data.lock().unwrap();\n<span class=\"c\">// guard holds exclusive access</span>\n<span class=\"c\">// other threads block until guard drops</span></code></div><p>Rust&#39;s <code>Mutex</code> owns the data it protects. Code cannot access the data without acquiring the lock. This is different from C++, where a mutex and the data it protects are separate objects, and the programmer must remember to lock before accessing.</p><p><strong>Mutability upgrade.</strong> The code requests a shared coordinate where it needs an exclusive one. <code>.iter()</code> → <code>.iter_mut()</code>. <code>&</code> → <code>&mut</code>. <code>.get()</code> → <code>.get_mut()</code>. Adding <code>mut</code> to a binding.</p><div class=\"cb\"><code><span class=\"k\">let</span> <span class=\"k\">mut</span> v = <span class=\"m\">vec!</span>[<span class=\"n\">1</span>, <span class=\"n\">2</span>, <span class=\"n\">3</span>];\n<span class=\"k\">for</span> item <span class=\"k\">in</span> v.iter_mut() {\n    *item += <span class=\"n\">1</span>;\n}</code></div><p><strong>Manual assertion: raw pointer deref.</strong></p><p><code>*const T</code> and <code>*mut T</code> bypass the aliasing rule entirely. The programmer asserts that no conflicting access exists. The <code>miri</code> interpreter can check many of these assertions at runtime during testing.</p>"},"p3_8":{"t":"Data Shapes and the Table","ctx":"Ownership forms a tree. Not all data fits a tree. The table maps common shapes to the dimension that is over-constrained.","body":"<table><thead><tr><th>Data shape</th><th>Over-constrained dimension</th><th>Spectrum position</th></tr></thead><tbody><tr><td>DAG, shared config</td><td>SPACE — multiple paths, one owner</td><td>Clone (type-level) or Rc/Arc (runtime)</td></tr><tr><td>Graph, doubly linked list</td><td>SPACE — cycles, back-pointers</td><td>Arena indices (type-level)</td></tr><tr><td>Cache, lazy init</td><td>COORDINATES — mutation behind &amp;self</td><td>Cell/RefCell (runtime)</td></tr><tr><td>Observer, event system</td><td>SPACE + COORDINATES — multiple observers</td><td>Rc + RefCell or channels</td></tr><tr><td>Thread communication</td><td>TIME — access serialized across threads</td><td>Channels (runtime) or Arc + Mutex</td></tr><tr><td>Self-referential struct</td><td>COORDINATES — coordinate to own field</td><td>Pin (type-level) or restructure</td></tr><tr><td>LRU cache</td><td>SPACE + COORDINATES — hash map + list reach same entry</td><td>Arena indices (type-level)</td></tr><tr><td>Simulation (particles + grid)</td><td>COORDINATES — two fields of same struct</td><td>Split fields (type-level)</td></tr></tbody></table><p>The left column is the shape. The middle column is which row of the <span class=\"ea\" data-t=\"p3_4\">mirror<span class=\"tt\"><span class=\"em-q\">Three dimensions, three columns of fixes.</span></span></span> to look at. The right column is how far right on the spectrum to slide. Most problems have a type-level solution. Slide to runtime only when the type-level solution is too restrictive. Slide to unsafe only when the runtime solution is too expensive.</p>"},"p3_9":{"t":"When to Reach for Unsafe","ctx":"The six patterns cover most programs. Unsafe serves the remainder.","body":"<p>The standard library uses <code>unsafe</code> internally for <code>Vec</code>, <code>HashMap</code>, <code>BTreeMap</code>, <code>String</code>, and many other types. The safe APIs that callers use are the product of the &quot;encode invariants in types&quot; <span class=\"ea\" data-t=\"p3_4\">escape hatch<span class=\"tt\"><span class=\"em-q\">Type-level encoding, runtime checking, or manual assertion.</span></span></span>, where <code>unsafe</code> internals expose a safe public interface.</p><p>Programs reach for <code>unsafe</code> in four situations. The safe patterns impose unacceptable performance costs. A data structure&#39;s invariants are provably correct but inexpressible in the type system. FFI boundaries lie outside Rust&#39;s ownership model. Intrusive data structures have nodes that contain their own link pointers.</p><p>The programmer manually asserts the <span class=\"ea\" data-t=\"p1_13\">S×T×C<span class=\"tt\"><span class=\"em-q\">Where data lives. When things happen. How you reach it.</span></span></span> invariants that no safe pattern can express. The assertion covers the same three dimensions the compiler checks in safe code. SPACE requires the allocation to be valid. TIME requires the access to happen while the space exists. COORDINATES requires the references to satisfy the aliasing rule.</p><div class=\"cb\"><code><span class=\"c\">/// SAFETY:</span>\n<span class=\"c\">/// SPACE    — `ptr` was allocated by `alloc::alloc` with `layout`</span>\n<span class=\"c\">/// TIME     — the allocation has not been deallocated</span>\n<span class=\"c\">/// COORD    — no other `&amp;mut` to this memory exists</span>\n<span class=\"k\">unsafe</span> {\n    <span class=\"k\">let</span> value = &amp;<span class=\"k\">mut</span> *ptr;\n    *value = <span class=\"n\">42</span>;\n}</code></div><p>The <code>unsafe</code> keyword marks these assertions as visible and searchable in the codebase, and the <code>miri</code> interpreter can check many of them at runtime during testing.</p>"},"p3_10":{"t":"Hints and Patterns Correspond","ctx":"Hints are reactive. Patterns are proactive. Same adjustments along the same three axes.","body":"<p>The compiler hints from the previous chapters are the reactive version of the patterns described in this chapter. The compiler rejected your code, and the hint tells you which dimension to adjust. The patterns are the proactive version. You are designing a program, and the pattern tells you how to structure it so the compiler accepts it from the start.</p><table><thead><tr><th>Hint (reactive)</th><th>Pattern (proactive)</th><th>Dimension</th></tr></thead><tbody><tr><td>Duplication</td><td>Clone &amp; Diverge</td><td>SPACE</td></tr><tr><td>Ownership Transfer</td><td>Message Passing</td><td>TIME</td></tr><tr><td>Reordering</td><td>Separate Read/Write Passes</td><td>TIME</td></tr><tr><td>Partitioning</td><td><span class=\"ea\" data-t=\"p3_7\">Split &amp; Partition<span class=\"tt\"><span class=\"em-q\">split_at_mut, struct fields, closures.</span></span></span></td><td>COORDINATES</td></tr><tr><td>Mutability Upgrade</td><td>Interior Mutability</td><td>COORDINATES</td></tr></tbody></table><p>A programmer who internalizes the patterns will recognize the hints when the compiler suggests them, because both are adjustments along the same <span class=\"ea\" data-t=\"p1_13\">three axes<span class=\"tt\"><span class=\"em-q\">Where data lives. When things happen. How you reach it.</span></span></span>.</p>"}},"edges":[{"from":"p1_1","to":"p1_1b","t":"What does Rust syntax look like?","w":"Familiar C++ syntax, then the programs Rust rejects.","card":true},{"from":"p1_1","to":"p1_2","t":"What doesn't compile?","w":"The compiler rejects code that looks right.","card":true},{"from":"p1_1","to":"p1_5","t":"Why is memory hard?","w":"Cores, caches, and stale data.","card":true},{"from":"p1_1","to":"p2_26","t":"borrow checker","w":"How does the compiler enforce the constraint?","card":false},{"from":"p1_1b","to":"p1_2","t":"What doesn't compile?","w":"The compiler rejects code that looks right.","card":true},{"from":"p1_1b","to":"p1_5","t":"Why is memory hard?","w":"Three primitives rooted in physics.","card":true},{"from":"p1_1b","to":"p1_2","t":"does not compile in Rust","w":"The compiler rejects code that looks right.","card":false},{"from":"p1_2","to":"p1_3","t":"Does the compiler catch thread bugs too?","w":"Does the type system prevent concurrency bugs?","card":true},{"from":"p1_2","to":"p1_4","t":"What's a dangling reference?","w":"C++ compiles it. Rust refuses. The details reveal why.","card":true},{"from":"p1_2","to":"p1_5","t":"Why do these bugs exist at all?","w":"Three primitives rooted in physics.","card":true},{"from":"p1_2","to":"p2_14","t":"ownership","w":"Why does assigning a value invalidate the original?","card":false},{"from":"p1_2","to":"p2_26","t":"borrowing rule","w":"How does the compiler know this is wrong?","card":false},{"from":"p1_2","to":"p1_4","t":"dangling reference","w":"C++ compiles it. Rust refuses.","card":false},{"from":"p1_2","to":"p1_18","t":"data race","w":"Two threads, one address, no synchronization.","card":false},{"from":"p1_2","to":"p2_16","t":"lifetime annotation","w":"An explicit marker that tells the compiler how long a reference lives.","card":false},{"from":"p1_3","to":"p1_4","t":"What does a dangling reference look like?","w":"The same bug, Rust vs C++, side by side.","card":true},{"from":"p1_3","to":"p1_5","t":"Why do all these bugs keep happening?","w":"Three primitives rooted in physics.","card":true},{"from":"p1_3","to":"p1_18","t":"What exactly is a data race?","w":"Two threads, one address, no synchronization.","card":true},{"from":"p1_3","to":"p2_14","t":"ownership","w":"Where does the value go, and why can't the old scope see it?","card":false},{"from":"p1_3","to":"p2_26","t":"borrowing rule","w":"How does one rule prevent both single-threaded and multi-threaded bugs?","card":false},{"from":"p1_3","to":"p1_18","t":"data races","w":"Two threads accessing the same memory, at least one writing.","card":false},{"from":"p1_4","to":"p1_12","t":"Why do these bugs exist?","w":"Physics creates distance. Distance forces copies.","card":true},{"from":"p1_4","to":"p1_2","t":"Are there more rejected programs?","w":"What other patterns does the compiler forbid?","card":true},{"from":"p1_4","to":"p2_4","t":"How does the compiler detect this?","w":"The compiler builds a control flow graph and checks every path.","card":true},{"from":"p1_4","to":"p2_2","t":"reclaimed","w":"The space is returned to the stack. The bytes are still there, but the allocation has ended.","card":false},{"from":"p1_4","to":"p1_16","t":"dangling pointer","w":"One of the most common memory bugs in C and C++.","card":false},{"from":"ds_0","to":"p1_12","t":"Where does distribution start?","w":"Physics creates distance. Distance forces copies.","card":true},{"from":"ds_0","to":"p1_8","t":"What is the constraint?","w":"Many readers or one writer.","card":true},{"from":"p1_12","to":"p1_13","t":"What are the three primitives?","w":"The physics made precise.","card":true},{"from":"p1_12","to":"p1_20","t":"How do languages solve this?","w":"Seven paradigms and their coherence strategies.","card":true},{"from":"p1_13","to":"p1_5b","t":"What happens when a copy goes stale?","w":"The coherence problem.","card":true},{"from":"p1_13","to":"p1_14","t":"How do real bugs map to S×T×C?","w":"Every memory bug is a specific interaction failure.","card":true},{"from":"p1_13","to":"p1_20","t":"How do language features map to S×T×C?","w":"Each feature targets a specific interaction between primitives.","card":true},{"from":"p1_5b","to":"p1_8","t":"What is the solution?","w":"Many readers or one writer. And nothing better exists.","card":true},{"from":"p1_5b","to":"p1_14","t":"How do bugs relate to staleness?","w":"Every memory bug is a specific S×T×C interaction failure.","card":true},{"from":"p1_8","to":"p1_8b","t":"Can anything escape the rule?","w":"Every escape route requires a runtime. The compiler has none.","card":true},{"from":"p1_8","to":"p1_5","t":"How does hardware enforce this?","w":"CPU caches enforce the same rule.","card":true},{"from":"p1_8","to":"p2_26","t":"How does Rust enforce this?","w":"The borrowing rule is this constraint at compile time.","card":true},{"from":"p1_8b","to":"p1_5","t":"How does hardware enforce this?","w":"CPU caches enforce the same rule, at runtime.","card":true},{"from":"p1_8b","to":"p2_26","t":"What is the borrow checker?","w":"The compile-time enforcement of the readers-writers rule.","card":true},{"from":"p1_8b","to":"p2_26","t":"borrow checker","w":"The compile-time rule that prevents these bugs.","card":false},{"from":"p1_5","to":"p1_6","t":"What happens step by step?","w":"Two cores, one address, the protocol in detail.","card":true},{"from":"p1_5","to":"p1_9","t":"What states does the protocol need?","w":"Four states derived from the constraint.","card":true},{"from":"p1_5","to":"p1_8","t":"What's the constraint?","w":"Many readers or one exclusive writer.","card":true},{"from":"p1_6","to":"p1_9","t":"What states does the protocol need?","w":"Four states derived from the constraint.","card":true},{"from":"p1_6","to":"p1_8","t":"What's the constraint?","w":"Many readers or one exclusive writer.","card":true},{"from":"p1_9","to":"p1_10","t":"How does the full protocol work?","w":"Every transition preserving the constraint.","card":true},{"from":"p1_9","to":"p1_8","t":"Remind me of the constraint","w":"Many readers or one exclusive writer.","card":true},{"from":"p1_9","to":"p1_5","t":"cache coherence protocol","w":"Caches enforce the rule at runtime.","card":false},{"from":"p1_10","to":"p1_10b","t":"Why does the protocol have four states?","w":"What each state means in plain language.","card":true},{"from":"p1_10","to":"p1_9","t":"How were the states derived?","w":"Four states from the constraint.","card":true},{"from":"p1_10","to":"p1_8","t":"constraint","w":"Many readers or one exclusive writer.","card":false},{"from":"p1_10b","to":"p1_21","t":"Where does this rule appear historically?","w":"Four decades, four domains, the same answer.","card":true},{"from":"p1_10b","to":"p1_11","t":"Is shared memory really message passing?","w":"Writing triggers a message-passing protocol underneath.","card":true},{"from":"p1_21","to":"p1_11","t":"Is shared memory really message passing?","w":"Writing triggers a message-passing protocol underneath.","card":true},{"from":"p1_21","to":"p2_1","t":"How does the compiler see a dangling reference?","w":"Bindings, addresses, and the analysis.","card":true},{"from":"p1_21","to":"p1_8","t":"Remind me of the coherence constraint","w":"The algebra of read/write conflicts.","card":true},{"from":"p1_21","to":"p2_26","t":"borrowing rules","w":"The constraint at compile time.","card":false},{"from":"p1_11","to":"p1_14","t":"How do bugs map to S×T×C?","w":"Every memory bug is a specific interaction failure.","card":true},{"from":"p1_11","to":"p1_13","t":"What are the three primitives?","w":"The physics made precise.","card":true},{"from":"p1_11","to":"p2_1","t":"How does the compiler see a dangling reference?","w":"Bindings, addresses, and the analysis.","card":true},{"from":"p1_14","to":"p1_15","t":"What does use-after-free look like?","w":"A heap pointer followed after the heap is freed.","card":true},{"from":"p1_14","to":"p1_16","t":"What does a dangling pointer look like?","w":"A reference that outlives the thing it points to.","card":true},{"from":"p1_14","to":"p1_18","t":"What does a data race look like?","w":"Two threads writing the same memory with no synchronization.","card":true},{"from":"p1_14","to":"p1_20","t":"How do languages prevent these?","w":"Seven paradigms and their coherence strategies.","card":true},{"from":"p1_14","to":"p1_15","t":"Use-after-free","w":"A heap pointer followed after the heap is freed.","card":false},{"from":"p1_14","to":"p1_16","t":"Dangling pointer","w":"A reference that outlives the thing it points to.","card":false},{"from":"p1_14","to":"p1_17","t":"Double free","w":"The allocator's bookkeeping is corrupted.","card":false},{"from":"p1_14","to":"p1_18","t":"Data race","w":"Two threads writing the same memory with no synchronization.","card":false},{"from":"p1_15","to":"p1_16","t":"How is a dangling pointer different?","w":"Same mechanism, but the target goes out of scope instead of being freed.","card":true},{"from":"p1_15","to":"p1_17","t":"What if you free the same memory twice?","w":"The allocator's bookkeeping corrupts.","card":true},{"from":"p1_15","to":"p2_14","t":"ownership","w":"One owner. When the owner goes out of scope, the value is dropped.","card":false},{"from":"p1_16","to":"p1_17","t":"What if you free the same memory twice?","w":"The allocator's bookkeeping corrupts.","card":true},{"from":"p1_16","to":"p1_15","t":"How is use-after-free related?","w":"Explicit free instead of scope end. Same stale coordinate.","card":true},{"from":"p1_16","to":"p1_18","t":"What happens when two threads hit the same memory?","w":"Parallel access with no coordination.","card":true},{"from":"p1_16","to":"p1_4","t":"the first chapter","w":"Rust rejects it at compile time. C++ prints garbage.","card":false},{"from":"p1_17","to":"p1_18","t":"What happens with parallel access?","w":"Two threads, one address, no synchronization.","card":true},{"from":"p1_17","to":"p1_15","t":"How is use-after-free related?","w":"Access after free instead of freeing twice.","card":true},{"from":"p1_17","to":"p1_19","t":"What other memory bugs exist?","w":"Buffer overflow, uninitialized read, memory leak.","card":true},{"from":"p1_17","to":"p2_14","t":"single ownership","w":"Each value has exactly one owner. The owner frees the value once, automatically.","card":false},{"from":"p1_18","to":"p1_19","t":"What other memory bugs exist?","w":"Buffer overflow, uninitialized read, memory leak.","card":true},{"from":"p1_18","to":"p1_20","t":"How do languages prevent data races?","w":"Seven paradigms and their coherence strategies.","card":true},{"from":"p1_18","to":"p1_8","t":"coherence problem","w":"The same algebra governs hardware and software.","card":false},{"from":"p1_18","to":"p2_26","t":"borrowing rule","w":"If any coordinate can write, no other coordinate can exist.","card":false},{"from":"p1_19","to":"p1_20","t":"How do languages deal with all of this?","w":"Seven paradigms, each with a different coherence strategy.","card":true},{"from":"p1_19","to":"p1_14","t":"How do all seven bugs compare?","w":"The full table mapping every bug to S×T×C interactions.","card":true},{"from":"p1_19","to":"p2_14","t":"ownership","w":"When the owner goes out of scope, the value is dropped automatically.","card":false},{"from":"p1_20","to":"p1_20b","t":"Which features address which dimensions?","w":"Each feature targets a specific S×T×C interaction.","card":true},{"from":"p1_20","to":"p1_21","t":"Where does this constraint come from historically?","w":"Four decades, four domains, the same answer.","card":true},{"from":"p1_20","to":"p2_1","t":"How does the compiler see a dangling reference?","w":"Bindings, addresses, and the analysis.","card":true},{"from":"p1_20","to":"p1_14","t":"Which bugs does each feature prevent?","w":"The bug table maps every category to S×T×C.","card":true},{"from":"p1_20","to":"p1_13","t":"three primitives","w":"Where data lives. When things happen. How you reach it.","card":false},{"from":"p1_20","to":"p1_20b","t":"specific interaction between primitives","w":"Which S×T×C interaction does each feature address?","card":false},{"from":"p1_20b","to":"p1_21","t":"Where does this constraint come from historically?","w":"Four decades, four domains, the same answer.","card":true},{"from":"p1_20b","to":"p1_14","t":"Which bugs does each feature prevent?","w":"The bug table maps every category to S×T×C.","card":true},{"from":"p1_20b","to":"p2_14","t":"Ownership","w":"Each value has one owner. Passing transfers ownership.","card":false},{"from":"p1_20b","to":"p2_16","t":"Lifetime annotations","w":"An explicit marker that tells the compiler how long a reference lives.","card":false},{"from":"p1_20b","to":"p2_26","t":"Rust borrow checker","w":"One writer or many readers at compile time. No runtime cost.","card":false},{"from":"p2_1","to":"p2_2","t":"Where does the space live?","w":"Stack vs heap, and the gap between language and machine.","card":true},{"from":"p2_1","to":"p2_4","t":"How does the compiler find the bug?","w":"The control flow graph and path analysis.","card":true},{"from":"p2_1","to":"p1_4","t":"dangling reference","w":"Rust rejects it. C++ prints garbage.","card":false},{"from":"p2_2","to":"p2_3","t":"What does the compiler need to see?","w":"Data flow and reclamation across scope boundaries.","card":true},{"from":"p2_2","to":"p2_14","t":"How does ownership tie heap to scope?","w":"One owner, scope-tied lifetime.","card":true},{"from":"p2_2","to":"p1_15","t":"use-after-free","w":"A heap pointer followed after the heap is freed.","card":false},{"from":"p2_2","to":"p2_14","t":"Ownership","w":"Heap lifetime follows owner scope.","card":false},{"from":"p2_3","to":"p2_4","t":"How does the compiler detect access to reclaimed space?","w":"Path analysis on the control flow graph.","card":true},{"from":"p2_3","to":"p2_16","t":"What are lifetime annotations?","w":"Declaring data flow across function boundaries.","card":true},{"from":"p2_3","to":"p2_16","t":"Lifetime annotations","w":"Declaring which input a borrowed output connects to.","card":false},{"from":"p2_4","to":"p2_5","t":"Why don't C++ compilers do this?","w":"Three properties prevent building the complete graph.","card":true},{"from":"p2_4","to":"p2_9","t":"Where do branches break the analysis?","w":"Branches create paths that depend on runtime values.","card":true},{"from":"p2_4","to":"p1_4","t":"dangling reference","w":"Rust rejects it at compile time.","card":false},{"from":"p2_4","to":"p2_11","t":"sound","w":"Never accept unsafe code.","card":false},{"from":"p2_5","to":"p2_6","t":"What's the deeper problem?","w":"Two obstacles that any language must address.","card":true},{"from":"p2_5","to":"p2_13","t":"How does Rust fix unconstrained pointers?","w":"Constrained references with known origins.","card":true},{"from":"p2_5","to":"p2_13","t":"constrains coordinate creation","w":"Every reference has a known target.","card":false},{"from":"p2_5","to":"p2_6","t":"two deeper problems","w":"Flat RAM and cross-scope data flow.","card":false},{"from":"p2_6","to":"p2_7","t":"What about data that crosses scope boundaries?","w":"The scope tree tracks names, not data flow.","card":true},{"from":"p2_6","to":"p2_13","t":"How does Rust constrain coordinates?","w":"References are typed relationships, not numbers.","card":true},{"from":"p2_6","to":"p2_13","t":"constrain how coordinates are created","w":"Every reference has a known target.","card":false},{"from":"p2_6","to":"p2_7","t":"data that crosses scope boundaries","w":"The scope tree tracks names, not data flow.","card":false},{"from":"p2_7","to":"p2_8","t":"What about types that contain pointers?","w":"Byte-copying a String duplicates the coordinate.","card":true},{"from":"p2_7","to":"p2_16","t":"How does Rust declare data flow?","w":"Lifetime annotations at function signatures.","card":true},{"from":"p2_7","to":"p2_16","t":"declare how data flows between scopes","w":"Lifetime annotations declare which input a borrowed output connects to.","card":false},{"from":"p2_7","to":"p2_8","t":"Types that contain pointers","w":"Byte-copying a String duplicates the coordinate without duplicating the space.","card":false},{"from":"p2_8","to":"p2_8b","t":"How do other languages handle this?","w":"Five strategies from doing nothing to compile-time proofs.","card":true},{"from":"p2_8","to":"p2_9","t":"Even with all obstacles patched, what remains?","w":"Branches create paths that depend on runtime values.","card":true},{"from":"p2_8","to":"p2_19","t":"How does Rust prevent invisible duplication?","w":"Move by default.","card":true},{"from":"p2_8","to":"p2_19","t":"prevents accidental byte-copying","w":"Why does Rust move by default instead of copying?","card":false},{"from":"p2_8","to":"p2_8b","t":"Other languages","w":"Five strategies, from doing nothing to compile-time proofs.","card":false},{"from":"p2_8b","to":"p2_9","t":"Even with all obstacles patched, what remains?","w":"Branches create paths that depend on runtime values.","card":true},{"from":"p2_8b","to":"p1_20","t":"Which paradigms use which strategies?","w":"Seven languages and their coherence strategies.","card":true},{"from":"p2_8b","to":"p2_10","t":"fundamental limit","w":"Not just hard. Mathematically impossible.","card":false},{"from":"p2_8b","to":"p1_20","t":"paradigm","w":"Seven languages, seven strategies.","card":false},{"from":"p2_9","to":"p2_10","t":"Can any algorithm predict which path executes?","w":"No. Turing proved it in 1936.","card":true},{"from":"p2_9","to":"p2_4","t":"Remind me how the analysis handles branches","w":"The compiler checks all paths and rejects when any is unsafe.","card":true},{"from":"p2_9","to":"p2_4","t":"analysis checks all paths","w":"The compiler rejects when any path is unsafe.","card":false},{"from":"p2_9","to":"p2_10","t":"no algorithm can avoid this limitation","w":"Turing proved it in 1936.","card":false},{"from":"p2_10","to":"p2_11","t":"What does the compiler do about this?","w":"Choose between two failures: accept unsafe or reject safe.","card":true},{"from":"p2_10","to":"p3_4","t":"What are the escape hatches?","w":"Restructure, encode in types, runtime checks, or unsafe.","card":true},{"from":"p2_10","to":"p2_11","t":"choose between two failures","w":"Sound or complete.","card":false},{"from":"p2_10","to":"p3_4","t":"Escape hatches","w":"Restructure, encode in types, runtime checks, or unsafe.","card":false},{"from":"p2_11","to":"p2_12","t":"What exactly is approximate?","w":"Names are exact. Data flow is approximated.","card":true},{"from":"p2_11","to":"p2_26","t":"What does the approximation look like?","w":"The borrowing rule.","card":true},{"from":"p2_11","to":"p1_8b","t":"readers-writers rule","w":"The compiler has no runtime. The readers-writers rule is the only coherence policy enforceable without one.","card":false},{"from":"p2_11","to":"p2_26","t":"approximation","w":"The borrowing rule.","card":false},{"from":"p2_12","to":"p2_13","t":"How does Rust patch each obstacle?","w":"Constrained references, ownership, signatures, move-by-default.","card":true},{"from":"p2_12","to":"p2_6","t":"Flat RAM","w":"The machine provides no structure for the compiler.","card":false},{"from":"p2_12","to":"p2_7","t":"cross-scope data flow","w":"The scope tree tracks names, not data flow.","card":false},{"from":"p2_12","to":"p2_8","t":"invisible duplication","w":"Byte-copying duplicates coordinates.","card":false},{"from":"p2_12","to":"p2_10","t":"undecidability","w":"Turing proved it in 1936.","card":false},{"from":"p2_13","to":"p2_14","t":"How does the compiler know when heap space dies?","w":"One owner, scope-tied lifetime.","card":true},{"from":"p2_13","to":"p2_5","t":"Why couldn't C++ do this?","w":"Three properties prevent the analysis.","card":true},{"from":"p2_13","to":"p2_6","t":"RAM is flat","w":"The machine provides no structure.","card":false},{"from":"p2_14","to":"p2_15","t":"How do you see what the compiler sees?","w":"The notation makes every operation explicit.","card":true},{"from":"p2_14","to":"p2_19","t":"Why does assignment invalidate the source?","w":"Move by default prevents invisible duplication.","card":true},{"from":"p2_15","to":"p2_16","t":"How do lifetime annotations work?","w":"Declaring data flow across function boundaries.","card":true},{"from":"p2_15","to":"p2_22","t":"How do all the pieces fit together?","w":"Composing the vocabulary.","card":true},{"from":"p2_15","to":"p2_21","t":"exclusive coordinate","w":"One coordinate, read and write access.","card":false},{"from":"p2_16","to":"p2_17","t":"How do operators connect to lifetimes?","w":"Desugaring and lifetime elision.","card":true},{"from":"p2_16","to":"p2_3","t":"What does the compiler need to see?","w":"Data flow and reclamation across boundaries.","card":true},{"from":"p2_17","to":"p2_18","t":"What can a coordinate actually do?","w":"Using data vs managing space.","card":true},{"from":"p2_17","to":"p2_26","t":"What's the borrowing rule?","w":"The compiler blocks exclusive access while a coordinate exists.","card":true},{"from":"p2_18","to":"p2_19","t":"Why does assignment invalidate the source?","w":"Move by default prevents invisible duplication.","card":true},{"from":"p2_18","to":"p2_22","t":"How do all the pieces fit together?","w":"Composing the vocabulary.","card":true},{"from":"p2_19","to":"p2_20","t":"What does mut mean on a binding?","w":"Rebinding and writing are different operations.","card":true},{"from":"p2_19","to":"p2_8","t":"Why is invisible duplication a problem?","w":"Two owners, one space.","card":true},{"from":"p2_19","to":"p2_8","t":"invisible coordinate duplication","w":"Byte-copying duplicates the coordinate without duplicating the space.","card":false},{"from":"p2_20","to":"p2_21","t":"What are shared and exclusive coordinates?","w":"The coherence constraint at the language level.","card":true},{"from":"p2_20","to":"p2_22","t":"How do all the pieces fit together?","w":"Composing the vocabulary.","card":true},{"from":"p2_21","to":"p2_22","t":"How do all these pieces compose?","w":"at, take, owner, alias working together.","card":true},{"from":"p2_21","to":"p1_8","t":"Remind me of the coherence constraint","w":"Many readers or one exclusive writer.","card":true},{"from":"p2_21","to":"p1_8","t":"coherence constraint","w":"The same algebra governs hardware and software.","card":false},{"from":"p2_21","to":"p2_26","t":"borrowing rule","w":"Many shared or one exclusive.","card":false},{"from":"p2_22","to":"p2_23","t":"Can a coordinate dangle while its owner lives?","w":"Push can reallocate the buffer.","card":true},{"from":"p2_22","to":"p2_26","t":"What's the borrowing rule?","w":"The compiler blocks exclusive access while a coordinate exists.","card":true},{"from":"p2_22","to":"p2_26","t":"borrowing rule","w":"Many shared or one exclusive.","card":false},{"from":"p2_23","to":"p2_24","t":"Why does derived data need a sync strategy?","w":"Any derived value becomes stale when its source changes.","card":true},{"from":"p2_23","to":"p1_2","t":"Which chapter 1 error is this?","w":"Push while borrowing.","card":true},{"from":"p2_23","to":"p1_2","t":"push-while-borrowing error","w":"The compiler rejects code that looks right.","card":false},{"from":"p2_24","to":"p2_25","t":"Can the compiler tell which methods are dangerous?","w":"No. The distinction depends on runtime state.","card":true},{"from":"p2_24","to":"p1_12","t":"Where else does derived data appear?","w":"Every layer of computing.","card":true},{"from":"p2_24","to":"p1_12","t":"pattern","w":"Every copy is derived data.","card":false},{"from":"p2_25","to":"p2_26","t":"So what does the compiler actually block?","w":"Exclusive access while any coordinate exists.","card":true},{"from":"p2_25","to":"p2_10","t":"Why is this undecidable?","w":"Whether push reallocates depends on runtime values.","card":true},{"from":"p2_25","to":"p2_10","t":"undecidable","w":"No algorithm can determine this for all programs.","card":false},{"from":"p2_26","to":"p2_27","t":"Why does the rule apply to simple types too?","w":"The compiler cannot determine which types manage internal space.","card":true},{"from":"p2_26","to":"p2_28","t":"What does this look like in practice?","w":"Every chapter 1 program explained by the rule.","card":true},{"from":"p2_26","to":"p2_4","t":"earlier","w":"The compiler checks all paths and rejects when any is unsafe.","card":false},{"from":"p2_26","to":"p2_21","t":"shared coordinates","w":"Read access, multiple coordinates coexist.","card":false},{"from":"p2_27","to":"p2_28","t":"What does this look like in practice?","w":"Every chapter 1 program explained by the borrowing rule.","card":true},{"from":"p2_27","to":"p2_11","t":"Why can't the compiler be more precise?","w":"Sound means never accepting unsafe code.","card":true},{"from":"p2_27","to":"p2_10","t":"undecidable","w":"No algorithm answers correctly for all inputs.","card":false},{"from":"p2_27","to":"p2_24","t":"derived data","w":"Any derived value becomes stale when its source changes.","card":false},{"from":"p2_28","to":"p3_1","t":"When does the approximation reject valid code?","w":"The compiler's tracking granularity.","card":true},{"from":"p2_28","to":"p1_3","t":"What about thread safety?","w":"The same borrowing rule prevents data races.","card":true},{"from":"p2_28","to":"p1_18","t":"data race","w":"Two threads, one address, no synchronization.","card":false},{"from":"p2_28","to":"p1_3","t":"thread safety errors","w":"The same borrowing rule prevents data races.","card":false},{"from":"p3_1","to":"p3_2","t":"Why do compilers skip decidable cases?","w":"Predictability over precision.","card":true},{"from":"p3_1","to":"p3_3","t":"What happens across function calls?","w":"Signatures lose field-level information.","card":true},{"from":"p3_1","to":"p2_10","t":"halting problem","w":"Turing proved it in 1936.","card":false},{"from":"p3_2","to":"p3_3","t":"What happens across function calls?","w":"Signatures lose field-level information.","card":true},{"from":"p3_2","to":"p3_1","t":"What can the compiler distinguish?","w":"Struct fields, literal indices, computed indices.","card":true},{"from":"p3_3","to":"p3_4","t":"What's the full picture?","w":"The mirror: three dimensions, three columns of fixes.","card":true},{"from":"p3_3","to":"p2_16","t":"How do lifetime annotations help?","w":"Declaring data flow across function boundaries.","card":true},{"from":"p3_4","to":"p3_5","t":"How do you relax space?","w":"Clone, arena indices, Rc, Arc, raw pointers.","card":true},{"from":"p3_4","to":"p3_6","t":"How do you relax time?","w":"Reordering, scoping, RefCell, channels.","card":true},{"from":"p3_4","to":"p3_7","t":"How do you relax coordinates?","w":"split_at_mut, interior mutability, raw pointers.","card":true},{"from":"p3_4","to":"p2_14","t":"Ownership","w":"One owner, scope-tied lifetime.","card":false},{"from":"p3_4","to":"p2_13","t":"Constrained references","w":"Every reference has a known target.","card":false},{"from":"p3_4","to":"p2_26","t":"borrowing rule","w":"Many shared or one exclusive.","card":false},{"from":"p3_4","to":"p2_11","t":"sound","w":"Never accept unsafe code.","card":false},{"from":"p3_5","to":"p3_6","t":"How do you relax time?","w":"Reordering, scoping, RefCell, channels.","card":true},{"from":"p3_5","to":"p3_8","t":"Which pattern fits which data shape?","w":"The table mapping structures to conflicts and patterns.","card":true},{"from":"p3_5","to":"p2_14","t":"ownership","w":"One owner, scope-tied lifetime.","card":false},{"from":"p3_5","to":"p1_13","t":"three dimensions","w":"Where data lives. When things happen. How you reach it.","card":false},{"from":"p3_6","to":"p3_7","t":"How do you relax coordinates?","w":"split_at_mut, interior mutability, raw pointers.","card":true},{"from":"p3_6","to":"p3_8","t":"Which pattern fits which data shape?","w":"The table mapping structures to conflicts.","card":true},{"from":"p3_6","to":"p2_16","t":"Lifetime annotations","w":"Declaring data flow across function boundaries.","card":false},{"from":"p3_6","to":"p2_26","t":"borrowing rule","w":"Many shared or one exclusive.","card":false},{"from":"p3_6","to":"p2_14","t":"ownership","w":"One owner, scope-tied lifetime.","card":false},{"from":"p3_6","to":"p1_10","t":"coherence protocol","w":"The old holder surrenders the token, the new holder receives it.","card":false},{"from":"p3_7","to":"p3_8","t":"Which pattern fits which data shape?","w":"The table mapping structures to conflicts.","card":true},{"from":"p3_7","to":"p3_4","t":"Remind me of the mirror table","w":"Three dimensions, three columns of fixes.","card":true},{"from":"p3_7","to":"p2_26","t":"aliasing rule","w":"Many shared or one exclusive.","card":false},{"from":"p3_7","to":"p1_8","t":"constraint","w":"Many readers or one exclusive writer.","card":false},{"from":"p3_8","to":"p3_9","t":"When is none of this enough?","w":"Performance, inexpressible invariants, FFI, intrusive structures.","card":true},{"from":"p3_8","to":"p3_4","t":"Remind me of the mirror","w":"Three dimensions, three columns of fixes.","card":true},{"from":"p3_8","to":"p3_4","t":"mirror","w":"Three dimensions, three columns of fixes.","card":false},{"from":"p3_9","to":"p3_10","t":"How do hints and patterns correspond?","w":"Same adjustments, reactive vs proactive.","card":true},{"from":"p3_9","to":"p3_4","t":"Remind me of the escape hatches","w":"Three dimensions, three columns of fixes.","card":true},{"from":"p3_9","to":"p3_4","t":"escape hatch","w":"Type-level encoding, runtime checking, or manual assertion.","card":false},{"from":"p3_9","to":"p1_13","t":"S×T×C","w":"Where data lives. When things happen. How you reach it.","card":false},{"from":"p3_10","to":"p1_1","t":"Back to the beginning","w":"The course introduction and how to navigate.","card":true},{"from":"p3_10","to":"p3_4","t":"Remind me of the mirror","w":"Three dimensions, three columns of fixes.","card":true},{"from":"p3_10","to":"p3_8","t":"Remind me of the data shapes table","w":"Which pattern fits which data structure.","card":true},{"from":"p3_10","to":"p3_7","t":"Split & Partition","w":"split_at_mut, struct fields, closures.","card":false},{"from":"p3_10","to":"p1_13","t":"three axes","w":"Where data lives. When things happen. How you reach it.","card":false}],"walks":{"textbook":{"chapters":[{"title":"Part 1: The Problem","nodes":["p1_1","p1_1b","p1_2","p1_3","p1_4","p1_14","p1_15","p1_16","p1_17","p1_18","p1_19","p1_20","p1_20b"]},{"title":"Part 2: Distributed Systems and Coherence","nodes":["ds_0","p1_12","p1_13","p1_5b","p1_8","p1_8b","p1_5","p1_6","p1_9","p1_10","p1_10b","p1_21","p1_11"]},{"title":"Part 3a: What the Compiler Needs","nodes":["p2_1","p2_2","p2_3","p2_4"]},{"title":"Part 3b: What Defeats the Analysis","nodes":["p2_5","p2_6","p2_7","p2_8","p2_8b","p2_9","p2_10","p2_11","p2_12"]},{"title":"Part 3c: Rust's Patches","nodes":["p2_13","p2_14","p2_15","p2_16","p2_17","p2_18","p2_19","p2_20","p2_21","p2_22"]},{"title":"Part 3d: The Borrowing Rule","nodes":["p2_23","p2_24","p2_25","p2_26","p2_27","p2_28"]},{"title":"Part 4: The Consequences","nodes":["p3_1","p3_2","p3_3","p3_4","p3_5","p3_6","p3_7","p3_8","p3_9","p3_10"]}]}}}